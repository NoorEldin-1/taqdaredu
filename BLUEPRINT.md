# BLUEPRINT.md — مخطط تقدر مقابل النظام القائم

> **ما هذه الوثيقة**
> قراءة مدمجة لوثيقتين مرجعيتين — **«مخطط النظام · الإصدار 1.0»** (26 فصلا)
> و**«فلو المستخدمين وخطة السبرنتات · الإصدار 1.0»** (9 فصول) — مسقطة على
> الشيفرة الفعلية في هذا المستودع سطرا بسطر.
>
> الهدف: أن يعرف من يفتح هذا الملف **ما المنصة المقصودة**، و**ما المبني منها
> فعلا**، و**أين الفجوة بالضبط** — قبل أن يكتب سطرا.
>
> مرجع البنية التقنية: [CLAUDE.md](CLAUDE.md). مرجع التشغيل المحلي: [SETUP.md](SETUP.md).
> هذه الوثيقة تكملهما ولا تكررهما: تلك تصف **كيف** بني، وهذه تصف **لماذا** وإلى **أين**.

---

## فهرس

| # | القسم |
|---|---|
| 0 | [الخلاصة التنفيذية](#0-الخلاصة-التنفيذية) |
| 1 | [الفكرة الحاكمة للمنتج](#1-الفكرة-الحاكمة-للمنتج) |
| 2 | [المعمارية: المخطط مقابل الواقع](#2-المعمارية-المخطط-مقابل-الواقع) |
| 3 | [نموذج البيانات](#3-نموذج-البيانات) |
| 4 | [الأدوار والصلاحيات](#4-الأدوار-والصلاحيات) |
| 5 | [قواعد العمل الست](#5-قواعد-العمل-الست) |
| 6 | [آلات الحالة](#6-آلات-الحالة) |
| 7 | [الواجهة البرمجية](#7-الواجهة-البرمجية) |
| 8 | [خط أنابيب المحتوى والفيديو](#8-خط-أنابيب-المحتوى-والفيديو) |
| 9 | [المدفوعات والمحفظة](#9-المدفوعات-والمحفظة) |
| 10 | [الإشعارات](#10-الإشعارات) |
| 11 | [البحث والاكتشاف](#11-البحث-والاكتشاف) |
| 12 | [الذكاء الاصطناعي](#12-الذكاء-الاصطناعي) |
| 13 | [كتالوج الأخطاء](#13-كتالوج-الأخطاء) |
| 14 | [نظام التصميم](#14-نظام-التصميم) |
| 15 | [جرد الشاشات الكامل](#15-جرد-الشاشات-الكامل) |
| 16 | [فلو المستخدمين الثلاثة](#16-فلو-المستخدمين-الثلاثة) |
| 17 | [السبرنتات الأربعة: ما أنجز وما بقي](#17-السبرنتات-الأربعة-ما-أنجز-وما-بقي) |
| 18 | [حالات الاختبار الحرجة الثمانية عشرة](#18-حالات-الاختبار-الحرجة-الثمانية-عشرة) |
| 19 | [الاختلافات المتعمدة عن المخطط](#19-الاختلافات-المتعمدة-عن-المخطط) |
| 20 | [الفجوات مرتبة بالأولوية](#20-الفجوات-مرتبة-بالأولوية) |
| 21 | [خريطة الملفات السريعة](#21-خريطة-الملفات-السريعة) |

---

## 0. الخلاصة التنفيذية

**النتيجة في سطر واحد:** الوثيقتان تصفان منصة على Laravel 12 + PostgreSQL +
Flutter + Next.js لسوق **مصري**. المبني فعلا هو منصة على **CodeIgniter 3 + MariaDB**
مصيرة من الخادم لسوق **سعودي** — لكن **قلب المنتج (بوابة الإتقان، المراجعة
المتباعدة، التوأم الرقمي، دفتر الأخطاء، المحفظة بالقيد المزدوج، حراسة النطاق)
منفذ بالحرف كما في المخطط**، على تقنية مختلفة.

| البعد | المخطط | الواقع | الحكم |
|---|---|---|---|
| **قواعد العمل (فصل 06)** | بوابة إتقان + مراجعة متباعدة | منفذة حرفيا في `Taqdar_repo_model.php` | ✅ مطابق |
| **نموذج البيانات (فصل 03)** | 30+ كيانا | ~24 جدولا من أصل 75 تطابق الأسماء والحقول | ✅ مطابق بفوارق تسمية |
| **الصلاحيات (فصل 05)** | RBAC بأربعة أدوار + نطاق في طبقة الاستعلام | `taqdar_role_helper.php` + `teacher_assignments` | ✅ مطابق |
| **الواجهة البرمجية (فصل 11)** | REST `/api/v1` بـ JWT | `Taqdar_gate.php` بجلسة CI3، 16 نقطة من ~45 | ⚠️ جزئي |
| **الفيديو والحماية (فصل 12)** | HLS + روابط موقعة + علامة مائية | YouTube/رابط مباشر، لا حماية | ❌ غائب |
| **المدفوعات (فصل 14)** | Paymob + webhook + idempotency | تحويل بنكي يدوي، لا بوابة مفعلة | ❌ غائب |
| **الذكاء الاصطناعي (فصل 13)** | RAG مقيد بالمنهج | غير موجود | ❌ غائب |
| **الشاشات (فصل 20)** | 19 طالب + 3 بوابات | 15 طالب + 11 معلم + 8 ولي أمر + لوحة إدارة | ✅ شبه كامل |

---

## 1. الفكرة الحاكمة للمنتج

### 1.1 الجملة الواحدة

> منصة تعليمية عربية تقيس **الإتقان** لا الحضور: الطالب لا ينتقل إلى الدرس
> التالي حتى يثبت فهم الحالي، والقفل **على الخادم** لا في الواجهة.

### 1.2 المفصل الهندسي: `objective`

المخطط (فصل 04) يبني كل شيء على قرار واحد: **السؤال يربط بـ `objective` لا
بالدرس**. هذا الربط وحده هو ما يجعل ثلاثة أنظمة ممكنة:

```
path ──1:n──> milestone ──1:n──> lesson ──1:n──> objective ──1:n──> question
                                                     │
                        ┌────────────────────────────┼────────────────────────────┐
                        ▼                            ▼                            ▼
                 بوابة الإتقان              المراجعة المتباعدة              التوأم الرقمي
              (أي مفهوم لم يتقن؟)          (ماذا نعيد ومتى؟)            (مستوى كل مهارة)
```

**في الواقع:** السلسلة كاملة موجودة. `question.objective_id` عمود فعلي
(`taqd_lms.sql`)، و`objectives` فيها `at_second` — ثانية شرح الهدف داخل الفيديو،
وهي ما يغذي `seek_to` في قرار البوابة.

**الفارق الوحيد:** `objectives.at_second` مملوءة بـ`0` في كل الصفوف المزروعة —
فـ`seek_to` يعيد الطالب إلى بداية الفيديو لا إلى موضع ضعفه. **العمود موجود
والمنطق يقرؤه، والبيانات ناقصة.**

### 1.3 المبادئ الأربعة التي لا تكسر

من فصل 09 في وثيقة السبرنتات — وهي القواعد التي تلتزم بها كل السبرنتات:

| # | القاعدة | حالة التطبيق |
|---|---|---|
| 1 | **المنطق دائما في الخادم** — القفل والخصم والدرجة | ✅ `Taqdar_gate.php` يحسم القفل، والواجهة تعرض فقط |
| 2 | **`Idempotency-Key` إلزامي على كل POST مالي** | ❌ غير موجود — لكن `wallet_entries.uq_we_ref` يمنع التكرار على مستوى القيد |
| 3 | **النطاق في طبقة الاستعلام لا في شرط عرض** | ✅ `teacher_owns_course()` · `parent_owns_child()` · `get_teacher_scope()` |
| 4 | **لا PR يقفل بلا اختبار لحالة حرجة** | ❌ لا اختبارات آلية في المستودع أصلا |
| 5 | **كل نص عربي في ملف ترجمة منفصل** | ❌ مخالف عمدا — النص العربي مكتوب في القوالب مباشرة |

---

## 2. المعمارية: المخطط مقابل الواقع

### 2.1 طبقات المخطط (فصل 01)

```
العملاء:   Flutter (جوال) · Next.js (طالب) · Web (معلم) · Web (إدارة)
الواجهة:   REST API v1 — مصادقة · تحديد معدل · تسجيل
الخدمات:   التعلم · التقييم · المحفظة · الحجز · الذكاء · الإشعارات
التخزين:   PostgreSQL · Redis · S3 · قاعدة متجهات
خارجي:     بوابة الدفع · بث الفيديو · مقابلات مباشرة · رسائل ودفع
```

### 2.2 التقنيات: المخطط مقابل الواقع (فصل 02)

| الطبقة | المخطط | الواقع | ملاحظة |
|---|---|---|---|
| تطبيق الجوال | Flutter 3.x | غير موجود | مسارات `/student/*` صممت لتطابق مسارات Flutter مستقبلا |
| الويب | Next.js | قوالب PHP مصيرة من الخادم | لا SPA ولا npm ولا خطوة بناء |
| الخادم | Laravel 12 | **CodeIgniter 3 على PHP 8.2** | فارق جوهري |
| قاعدة البيانات | PostgreSQL + JSONB + pgvector | **MariaDB**، JSON بـ`json_valid()` CHECK | `parent_links.scope` مثال |
| الذاكرة المؤقتة | Redis | كاش ملفات CI3 | `Taqdar_gate` يستعمله لتحديد المعدل |
| التخزين | S3 متوافق | `uploads/` محلي | `wasabi_*` في `settings` فارغة |
| الفيديو | HLS + CDN | YouTube / رابط مباشر | `lesson.video_type` |
| اللحظي | Laravel Reverb | غير موجود | |
| المقابلات | LiveKit | **Google Meet** — `tutoring_sessions.meet_url` | حل يدوي بسيط |
| المدفوعات | Paymob | **تحويل بنكي يدوي** | 16 بوابة في الجدول، **لا واحدة مفعلة لتقدر** |
| الذكاء | نموذج لغوي + RAG | غير موجود | |

### 2.3 البنية الفعلية: طبقتان في شجرة واحدة

هذا هو أهم ما لا تذكره الوثيقتان: النظام **ليس مكتوبا من الصفر**، بل مبني على
`Academy LMS` التجارية (Creativeitem) وموسع بطبقة `Taqdar_*`.

```
الطبقة الأصلية (Academy LMS)          طبقة تقدر (المنتج الحقيقي)
─────────────────────────────         ──────────────────────────────
Home.php · Admin.php                  Taqdar.php (2114 سطرا)
User.php · Api.php                    Taqdar_admin.php · Taqdar_gate.php
Crud_model.php (226 ك.ب)              Taqdar_repo_model.php (1526)
User_model.php                        Taqdar_wallet_model.php (1153)
                                      Taqdar_parent_model.php (1211)
                                      Taqdar_teacher_model.php (1003)
                                      Taqdar_billing_model.php (804)
                                      Taqdar_marking_model.php (795)
                                      Taqdar_sessions_model.php (621)
                                      Taqdar_events_model.php (475)
views/frontend/default-new            views/frontend/taqdar (78 قالبا)
assets/frontend · assets/backend      assets/taqdar
```

**القاعدة:** الميزات الجديدة تكتب في طبقة `Taqdar_*`. تعديل `Crud_model.php`
أو `Admin.php` يمس شيفرة مشتركة مع مسارات LMS الأصلية.

**الجسور الانتقالية** — أعمدة تربط النموذجين مؤقتا وتحذف بعد الهجرة:
- `paths.course_id` → `course.id`
- `milestones.section_id` → `section.id`
- `sync_watch_history()` في `Taqdar_repo_model` يزامن `lesson_progress` مع
  `watch_histories` الأصلي، فلا تفترق الشاشتان.

---

## 3. نموذج البيانات

### 3.1 جرد المطابقة الكامل (فصل 03 من المخطط)

`taqd_lms` فيها **75 جدولا**. الجدول التالي يقابل كيانات المخطط بالجداول الفعلية.

#### الكيانات الأساسية

| المخطط | الجدول الفعلي | الحالة | ملاحظة |
|---|---|---|---|
| `users` | `users` | ⚠️ مختلف | `first_name`/`last_name` لا `name`؛ **لا عمود `role`** |
| `students` | — | ❌ غائب | حقوله مبعثرة في `users`: `grade_id` · `age` · `guardian_email`. **لا `daily_goal_minutes` ولا `streak_days` ولا `streak_freeze_used_at`** |
| `teachers` | — | ❌ غائب | حقوله في `users`: `is_instructor` · `teacher_slug` · `teacher_stage` · `teacher_rating` · `teacher_reviews` · `teacher_courses`. **لا `verified_at` ولا `commission_rate`** (النسبة على `paths.teacher_share_percent` بدلها) |
| `teacher_assignments` | `teacher_assignments` | ✅ مطابق | `+ can_publish · can_take_sessions` كما في فصل 19 |
| `parent_links` | `parent_links` | ✅ مطابق | `+ triggerان` يمنعان `status=active` بلا `consent_at` |
| `subjects` / `grades` | `subjects` · `grades` | ✅ مطابق | `name_ar · name_en · order · active` |

#### المحتوى

| المخطط | الجدول الفعلي | الحالة | ملاحظة |
|---|---|---|---|
| `paths` | `paths` | ✅ مطابق+ | `+ slug · thumbnail · badge · level · category_id · teacher_share_percent · course_id · tq_order · tq_seed`. **`price` بالهللات كعدد صحيح** كما تأمر القاعدة |
| `milestones` | `milestones` | ✅ مطابق | `+ section_id` (جسر) |
| `lessons` | `lesson` (مفرد) | ⚠️ الأصلي | جدول Academy: `duration` نص `HH:MM:SS` لا `duration_sec`؛ `video_url` لا `video_asset_id`؛ `+ tq_status` |
| `objectives` | `objectives` | ✅ مطابق | `+ at_second` — وهو مفتاح `seek_to` |
| `attachments` | `resource_files` + `lesson.attachment` | ⚠️ الأصلي | |
| `books` / `bundles` | `books` · `plans` | ⚠️ مختلف | لا `bundle_items`؛ الباقات تحدد نطاقها بـ`plans.scope_ids` (قائمة معرفات صفوف) |

#### التقييم والتقدم

| المخطط | الجدول الفعلي | الحالة | ملاحظة |
|---|---|---|---|
| `questions` | `question` (مفرد) | ⚠️ الأصلي | جدول Academy + `objective_id` مضاف. الخيارات في عمود `options` JSON لا في جدول |
| `question_options` | — | ❌ غائب | مدمجة في `question.options` |
| `assessments` | `assessments` | ✅ مطابق | `type enum('review','quiz','exam','homework')` حرفيا |
| `attempts` | `attempts` | ✅ مطابق | `+ uq_attempt_no(assessment,student,attempt_no)` |
| `answers` | `answers` | ✅ مطابق | `+ uq_answer_attempt_question` — يمنع الازدواج |
| `lesson_progress` | `lesson_progress` | ✅ مطابق | `mastered_at` موجود — وهو ما تكتبه بوابة الإتقان |
| `review_queue` | `review_queue` | ✅ مطابق | `ease decimal(4,2)` — **عشري ثابت لا عائم** كما تأمر القاعدة |
| `skill_state` | `skill_state` | ✅ مطابق | `level` مقياسه **0..100** لا 0..1 كما في المخطط |

#### التجارة والحصص

| المخطط | الجدول الفعلي | الحالة | ملاحظة |
|---|---|---|---|
| `orders` / `order_items` | `subscriptions` · `subscription_items` · `invoices` | ⚠️ مختلف | النموذج **اشتراكي** لا طلبي: `plan_id` · `path_id` · `status(pending/active/cancelled/expired)` |
| `enrollments` | `enrol` | ⚠️ الأصلي | جدول Academy |
| `wallets` | `wallets` | ✅ مطابق+ | `balance_available · balance_pending · balance_locked` — **الثالث زيادة صحيحة** (فصل 07 يطلبه للسحب المجمد) |
| `wallet_entries` | `wallet_entries` | ✅ مطابق+ | `type · bucket · amount · ref · origin · subject · released_at · occurred_at`. `uq_we_ref` **فريد** — منع تكرار على مستوى القاعدة |
| `payouts` | `payout` | ⚠️ الأصلي | جدول Academy + `destination · requested_channel · amount_halalas`. **`status` عدد صحيح لا enum** |
| `availability_slots` | `availability_slots` | ✅ مطابق | `status enum('open','held','booked')` حرفيا |
| `sessions` | `tutoring_sessions` | ✅ مطابق+ | 7 حالات بدل 4: `requested·confirmed·declined·expired·live·completed·refunded`. `+ meet_url` · `context_objective_id` |

#### جداول تقدر التي لا يذكرها المخطط

| الجدول | ما هو |
|---|---|
| `audit_log` | سجل التدقيق — ملحق فقط. **مستثنى من تجريد التشكيل عمدا** |
| `plans` / `subscriptions` / `subscription_items` | النموذج الاشتراكي (بديل `orders`) |
| `invoices` | الفواتير — `next_invoice_no()` |
| `competitions` / `competition_entries` | المسابقات — ليست في المخطط أصلا |
| `tq_prefs_user` / `tq_prefs_notify` | تفضيلات المستخدم والإشعارات |
| `tq_auth_login_attempts` / `tq_auth_password_resets` | تحديد معدل الدخول واستعادة كلمة المرور |
| `applications` | طلبات انضمام المعلمين — `status · document · admin_note · reviewed_by` |
| `books` | كتب المنهج السعودي |
| `language` (1872 صفا) | ترجمات Academy إلى جانب `languages/` |
| `settings` (97+ صفا) | **مفاتيح بوابات الدفع و SMTP وثوابت المنتج تعيش هنا لا في الشيفرة** |

### 3.2 ثوابت المنتج في جدول `settings`

هذه هي «قواعد العمل القابلة للضبط» — لا ثوابت مدفونة في الشيفرة:

| المفتاح | القيمة | ما يضبطه | مرجع المخطط |
|---|---|---|---|
| `mastery_pass_mark` | `3` | عتبة النجاح في مراجعة الدرس (من 5) | فصل 06 |
| `mastery_review_questions` | `5` | عدد أسئلة مراجعة الدرس | فلو الطالب |
| `review_daily_batch` | `10` | دفعة المراجعة اليومية | فصل 06 |
| `review_max_interval_days` | `60` | سقف تباعد المراجعة | فصل 06 — **حرفيا** |
| `review_initial_ease` | `2.5` | السهولة الابتدائية | فصل 06 |
| `lesson_complete_ratio` | `0.9` | نسبة المشاهدة التي تعد الدرس مكتملا | — |
| `tq_pass_threshold` | `60` | عتبة نجاح الواجب المصحح (%) | — |
| `taqdar_refund_window_days` | `14` | نافذة الاسترداد | فصل 14 — **حرفيا** |
| `taqdar_payout_min_sar` | `100` | أقل مبلغ سحب | فصل 07 يقول 200 جنيه؛ هنا 100 ريال |
| `taqdar_teacher_share_default` | `15` | نسبة المعلم الافتراضية (%) | فصل 14 |
| `taqdar_path_default_days` | `180` | مدة صلاحية المسار | — |
| `taqdar_vat_percent` | فارغ | ضريبة القيمة المضافة | فصل 14 |
| `taqdar_stat_*` | أرقام | إحصاءات الصفحة الرئيسية المعروضة | — |

---

## 4. الأدوار والصلاحيات

### 4.1 مصفوفة المخطط (فصل 05)

| المورد | طالب | معلم | ولي أمر | أدمن | ملاحظة النطاق |
|---|---|---|---|---|---|
| الدروس المشتراة | قراءة | قراءة | — | الكل | الطالب: ما اشتراه فقط |
| إنشاء/تعديل المحتوى | — | كتابة | — | الكل | المعلم: مادته وصفه فقط |
| نشر المحتوى | — | طلب | — | اعتماد | النشر يمر بمراجعة |
| بنك الأسئلة | — | كتابة | — | الكل | أسئلته هو |
| الدرجات | قراءة | كتابة | قراءة | الكل | ولي الأمر: ابنه فقط |
| تقارير الطالب | قراءة | قراءة | قراءة | الكل | المعلم: طلابه فقط |
| محادثات الطالب الخاصة | كتابة | — | — | — | **لا أحد؛ يحتاج قرارا موثقا** |
| المحفظة | — | قراءة | — | الكل | محفظته هو |
| طلب سحب | — | إنشاء | — | اعتماد | الاعتماد للمالية |
| الدفع | إنشاء | — | إنشاء | قراءة | ولي الأمر ينوب عن ابنه |
| مواعيد الحصص | حجز | نشر/تأكيد | حجز | الكل | — |
| إسناد المواد والصفوف | — | — | — | كتابة | الإدارة حصرا |
| سجل التدقيق | — | — | — | قراءة | غير قابل للحذف |

### 4.2 التنفيذ الفعلي

**اشتقاق الدور:** `application/helpers/taqdar_role_helper.php`

المشكلة البنيوية: جدول `role` فيه **دوران فقط** — `Admin(1)` و`User(2)` —
وراية `is_instructor` متعامدة عليهما، **ولا دور لولي الأمر أصلا**. فالدور يشتق
في موضع واحد بترتيب مقصود:

```php
tq_role($user_id):
  1. لا user_id            → 'guest'
  2. status !== 1          → 'guest'
  3. role_id === 1         → 'admin'      // الأدمن أولا: قد يكون is_instructor=1 كذلك
  4. is_instructor         → 'teacher'    // الإعلان لا يكفي — الإدارة تضبطه عند الاعتماد
  5. tq_gate === 'parent'  → 'parent'     // إعلانه يكفي: بوابته لا تكشف شيئا بلا رابط
  6. parent_links نشط      → 'parent'
  7. غير ذلك               → 'student'
```

**عمود `users.tq_gate`** — قيمته `student|parent|teacher` — هو الدور المعلن عند
التسجيل. كان يكتب ولا يقرأ فيصنف كل من سجل طالبا؛ صحح.

**الحراس الثلاثة في `Taqdar.php`:**

| الحارس | السطر | ما يفعل |
|---|---|---|
| `require_login()` | 51 | جلسة أو تحويل إلى `/login` |
| `require_role($role)` | 65 | ينادي `tq_guard()` — **الأدمن لا يستثنى** |
| `write_guard($role)` | 803 | **POST وحده**؛ GET يرد `show_404()` |

**تحقق الملكية:**

| الدالة | السطر | ما تتحقق منه |
|---|---|---|
| `teacher_owns_course()` | 919 | الكورس ملك هذا المعلم |
| `teacher_owns_lesson()` | 935 | الدرس داخل كورس يملكه |
| `parent_owns_child()` | 946 | رابط **نشط** بهذا الابن |
| `get_teacher_scope()` | `Taqdar_repo_model:1336` | نطاق المعلم من `teacher_assignments` |
| `teacher_can()` | `Taqdar_repo_model:1366` | مادة + صف + قدرة (`can_publish` / `can_take_sessions`) |

**قاعدة `tq_guard` الحاكمة:** الأدمن الذي يفتح بوابة طالب يعاد إلى بوابته
برسالة مفهومة — لا يرى شاشات فارغة يظنها معطوبة، ولا يعطي اختباره نتيجة كاذبة.

### 4.3 الفجوات في الصلاحيات

- ❌ `permissions` جدول Academy الأصلي (`admin_id` + JSON) — **لا صلاحيات دقيقة
  داخل الإدارة**: الأدوار الفرعية التي يفترضها فصل 19 (`دعم+` · `مراجع+` ·
  `مالية` · `إشراف` · `إدارة`) غير موجودة. كل أدمن يرى كل شيء.
- ❌ لا منع فعلي لـ`UPDATE`/`DELETE` على `audit_log` **على مستوى صلاحيات قاعدة
  البيانات** — المخطط (فصل 19، والتاسك `B1.9`) يطلبه صراحة. الجدول ملحق
  بالاتفاق لا بالإجبار.

---

## 5. قواعد العمل الست

### 5.1 بوابة الإتقان — القاعدة الأم

**نص المخطط (فصل 06):**

```
ON assessment.submit WHERE type = 'review'
  score >= pass_mark
    → lesson_progress.mastered_at = now()
    → unlock next lesson
    → schedule objectives into review_queue (interval = 1 day)
  score < pass_mark AND attempt_no = 1
    → return { retry: true, seek_to: weakest_objective.timestamp }
  score < pass_mark AND attempt_no = 2
    → return { retry: true, alternate_explanation_id }
  score < pass_mark AND attempt_no >= 3
    → return { suggest_session: true, context_objective_id }
    → next lesson stays locked
```

**التنفيذ:** `Taqdar_repo_model::gate_decision()` — السطر 895.

| السلوك | مطابق؟ | التفصيل |
|---|---|---|
| نجاح ⇒ `mastered_at` | ✅ | `INSERT ... ON DUPLICATE KEY UPDATE mastered_at = COALESCE(...)` — لا يمحو إتقانا سابقا |
| نجاح ⇒ فتح التالي | ✅ | `unlocked_lesson_id` من `lesson_lock_state()` |
| نجاح ⇒ جدولة الأهداف | ✅ | `schedule_objectives($student, $lesson, 1)` — بفاصل يوم |
| محاولة 1 ⇒ `seek_to` | ✅ | من `weakest_objective()`: أقل نسبة صواب، وعند التساوي الأسبق زمنا |
| محاولة 2 ⇒ شرح بديل | ✅ | `alternate_explanation()` — يبحث عن هدف **بالنص نفسه** في درس آخر |
| محاولة 3+ ⇒ اقتراح حصة | ✅ | `suggest_session: true` + `context_objective_id` + `audit()` |
| القفل يبقى | ✅ | `next_lesson_locked: true` في الحالات الثلاث |

**زيادات صحيحة على المخطط:**
- الإتقان يمر بـ`sync_watch_history()` أيضا — فمن أتقن درسا دون بلوغ عتبة
  المشاهدة لا يبقى «غير مكتمل» في شاشة وليه ومعلمه.
- `audit('mastery.unlock')` و`audit('mastery.suggest_session')` — أثر في السجل.
- **لا حد أقصى للمحاولات** — المحاولة الرابعة تعطي السلوك نفسه كالثالثة.

**قاعدة الخادم الصارمة:** `GET /taqdar_gate/lesson/{id}` لدرس غير مفتوح يرجع
**403 `MASTERY_LOCKED` بلا رابط تشغيل**. والمسار القديم `home/lesson/...`
محول في `routes.php:231-232` إلى المشغل الجديد — لأنه كان **يلتف على القفل**:
لا يعرف بوابة الإتقان ويكتب في `watch_histories` وحده.

### 5.2 المراجعة المتباعدة

**نص المخطط:**

```
ON review.answer
  correct   → ease += 0.1 ; interval = round(interval * ease)
  incorrect → ease = max(1.3, ease - 0.2) ; interval = 1 ; lapses += 1
  due_at = now() + interval days
  cap interval <= 60 days
  daily batch = 10 questions, ordered by due_at then difficulty
```

**التنفيذ:** `Taqdar_repo_model::answer_review()` — السطر 1103. **مطابق حرفيا**،
والثوابت الثلاثة من `settings` لا من الشيفرة. زيادة صحيحة: كل إجابة تحرك
`touch_skill_state()` أيضا — فالتوأم الرقمي يتغذى من المراجعة كما من الاختبار.

### 5.3 دفتر الأخطاء

**قرار هندسي مهم غير مذكور في المخطط:** الدفتر **يشتق من `answers` حيث
`is_correct = 0`** — لا جدول مستقل. السبب المكتوب في الشيفرة: «حتى لا يفترق
الدفتر عن الحقيقة». (`get_mistakes()` — السطر 1162.)

### 5.4 التوأم الرقمي

`touch_skill_state()` — السطر 1197. يحدث `skill_state` بعد كل محاولة وكل مراجعة.
`level` مقياسه 0..100 (المخطط يقول 0..1 — فارق مقياس لا فارق معنى).

### 5.5 المحفظة

**نص المخطط (فصل 06 + 14):**

```
ON order.paid
  net = total - tax - payment_fees - discount
  teacher_share = net * commission_rate
  wallet_entries += { type:'sale', amount:teacher_share, released_at: now()+refund_window }
  → counts toward balance_pending
ON refund_window elapsed AND NOT refunded → move to balance_available
ON refund BEFORE release → reverse the entry entirely (never negative available)
```

**التنفيذ:** `Taqdar_wallet_model.php` (1153 سطرا).

| القاعدة | الدالة | مطابق؟ |
|---|---|---|
| قيد البيع معلقا | `post_sales()` · `credit_path_sale()` | ✅ |
| التحرير بعد النافذة | `release_matured()` | ✅ — `taqdar_refund_window_days = 14` |
| عكس القيد عند الاسترداد | `reverse_origin()` · `record_refund()` | ✅ — **قيد عكسي لا مسح** |
| لا رصيد سالب | `recompute()` + `audit_balances()` | ✅ |
| القيد المزدوج | `bucket_sum()` يحسب من القيود | ⚠️ **جزئي** — انظر أدناه |
| المبالغ بالهللات كأعداد صحيحة | كل الأعمدة `bigint` | ✅ |
| مهلة التعليق = الاسترداد + يومان | — | ❌ التعليق = نافذة الاسترداد بالضبط (14 يوما) |

**التحفظ على القيد المزدوج:** المخطط (فصل 14) يقول «المحفظة **دفتر قيود لا حقل
رصيد**». الواقع: `wallets` فيها ثلاثة أعمدة رصيد (`available` · `pending` ·
`locked`) **إلى جانب** دفتر `wallet_entries`. الأعمدة مشتقة و`recompute()`
يعيد حسابها من القيود، و`audit_balances()` يكشف الانحراف — لكنها **قابلة
للتعديل المباشر تقنيا**، وهو ما يحذر منه المخطط.

### 5.6 حجز الحصص

| قاعدة المخطط | الواقع |
|---|---|
| `slot.hold` 10 دقائق نافذة دفع | ⚠️ `held` موجودة، لكن **بلا دفع** — الحصة مجانية |
| `payment.success → confirmed` | ⚠️ `decide()` من **المعلم** لا من الدفع |
| انتهاء المهلة يحرر المقعد | ✅ `Taqdar_cron::expire_sessions()` كل ساعة |
| إلغاء الطالب: >24س كامل / <24س 50% / <2س صفر | ❌ غير منفذ — لا مال في الحصص |
| غياب المعلم: استرداد + علامة | ❌ غير منفذ |

**الحماية من التزامن موجودة:** `request_session()` يحدث `availability_slots`
بشرط `status = 'open'` **داخل التحديث نفسه** — فطلبان متزامنان لا يعلقان
موعدا واحدا مرتين.

---

## 6. آلات الحالة

### 6.1 الطلب `order` (فصل 07) → الاشتراك `subscription`

| المخطط | الواقع |
|---|---|
| `draft → pending → paid → fulfilled` | `pending → active → cancelled/expired` |
| `refunded` · `disputed` | ❌ غير موجودتين |
| مهلة 30 دقيقة ثم `expired` | ❌ — التعليق يبقى حتى يفعله الأدمن يدويا |

الانتقالات في `Taqdar_billing_model`: `subscribe()` · `activate()` ·
`activate_manually()` · `activate_from_gateway()` · `cancel()` · `expire_due()`.

### 6.2 الحصة `session`

| المخطط | الواقع `tutoring_sessions.status` |
|---|---|
| `requested → confirmed → live → completed` | ✅ الأربع موجودة |
| `declined` · `expired` · `refunded` | ✅ الثلاث موجودة |
| نافذة رد 12 ساعة | ⚠️ `Taqdar_cron_events::session_requests()` يذكر — التحقق من المهلة في `expire_sessions()` |
| الغياب يثبت من سجل `room.join` | ❌ لا سجل دخول — `meet_url` رابط Google Meet خارجي |

### 6.3 السحب `payout`

| قاعدة المخطط | الواقع |
|---|---|
| خصم الرصيد **عند الطلب** لا الاعتماد | ✅ `request_payout()` — تجميد فوري في `balance_locked` |
| طلبان متزامنان لا يتجاوزان الرصيد | ✅ منفذ |
| `amount >= min_payout` | ✅ `taqdar_payout_min_sar = 100` |
| `kyc_verified` شرط | ❌ **لا حقل توثيق أصلا** — `teachers.verified_at` غير موجود |
| `admin.approve → rail.settled → paid` | ⚠️ `mark_payout_paid()` · `cancel_payout()` — يدوي |
| سبب إلزامي عند الرفض | ⚠️ `cancel_payout()` بلا سبب إلزامي |

**حالة `payout.status` عدد صحيح لا enum** — ميراث Academy.

---

## 7. الواجهة البرمجية

### 7.1 ما يقوله المخطط (فصل 10 + 11)

| القاعدة | التطبيق المطلوب | الواقع |
|---|---|---|
| المصادقة | JWT Bearer · 15 دقيقة · refresh دوار | ❌ **جلسة CI3 (كوكي)** |
| الترقيم | cursor لا offset | ❌ `limit`/`offset` |
| عدم التكرار | `Idempotency-Key` على كل POST مالي | ❌ غير موجود |
| الإصدار | `/api/v1` في المسار | ❌ `/taqdar_gate/*` — واصف نفسه `v1-transitional` |
| التحديد | 60 طلبا/دقيقة · 10 لنقاط الدفع | ✅ منفذ بأرقام أخرى (انظر أدناه) |
| الوقت | UTC بصيغة ISO-8601 | ❌ `Asia/Riyadh` بصيغة MySQL |
| الأخطاء | غلاف موحد برمز ثابت | ✅ **منفذ** |
| كل استجابة تحمل الصلاحية `can:{edit,delete}` | | ❌ غير منفذ |
| لا يعود سر في استجابة | روابط تشغيل موقعة | ❌ روابط دائمة |

### 7.2 `Taqdar_gate.php` — البوابة الفعلية

**فلسفتها المكتوبة في رأس الملف:**
> ما يفعله هذا المتحكم لا يعاد فعله في العميل: القفل هنا، والتصحيح هنا، وقرار
> «أعد المحاولة» أو «اطلب حصة» هنا. القفل البصري في الواجهة تجميل.

**النقاط الـ16:**

| المسار | الطريقة | يقابل في المخطط |
|---|---|---|
| `/taqdar_gate/paths` | GET | `GET /api/v1/paths` |
| `/taqdar_gate/path/{id}` | GET | `GET /api/v1/paths/{id}` |
| `/taqdar_gate/lesson/{id}` | GET | `GET /api/v1/lessons/{id}` |
| `/taqdar_gate/progress` | POST | `POST /api/v1/lessons/{id}/progress` |
| `/taqdar_gate/review_start` | POST | — (يبدأ محاولة) |
| `/taqdar_gate/review_submit` | POST | `POST /api/v1/assessments/{id}/submit` |
| `/taqdar_gate/review_answers` | GET | — |
| `/taqdar_gate/reviews` | GET | `GET /api/v1/review/due` |
| `/taqdar_gate/review_answer` | POST | `POST /api/v1/review/answer` |
| `/taqdar_gate/mistakes` | GET | `GET /api/v1/mistakes` |
| `/taqdar_gate/skill_map` | GET | — (التوأم الرقمي) |
| `/taqdar_gate/wallet` | GET | `GET /api/v1/wallet` |
| `/taqdar_gate/children` | GET | `GET /api/v1/parent/children` |
| `/taqdar_gate/child/{id}` | GET | `GET /api/v1/parent/children/{id}` |
| `/taqdar_gate/teacher_scope` | GET | — |
| `/taqdar_gate/` | GET | وصف الخدمة |

**تحديد المعدل الفعلي** (نافذة منزلقة على كاش الملفات):

| الدلو | الحد | النافذة |
|---|---|---|
| `progress` | 120 | 60 ث |
| `review_start` | 20 | 60 ث |
| `review_submit` | 30 | 60 ث |
| `review_answer` | 60 | 60 ث |
| `read` (الافتراضي) | 120 | 60 ث |

**تفصيل هندسي يستحق الحفظ — `TQ-GATE-CLEAN`:** المتحكم يفتح `ob_start()` في
الباني ويغلقه في `respond()`. أي `echo` عارض (تنبيه PHP، سطر فارغ بعد `?>` في
مساعد) كان يجعل أول بايت في الرد `<` أو مسافة فيسقط `JSON.parse` في العميل
برسالة لا علاقة لها بالخطأ الحقيقي. المخزن يبتلعه ويسجله في سجل الأخطاء.

### 7.3 النقاط الغائبة عن `/api/v1` المخطط

كل هذه مطلوبة في فصل 11 وغير موجودة كنقاط JSON (بعضها موجود كشاشات مصيرة):

- المصادقة كاملة: `register` · `login` · `otp/request` · `otp/verify` ·
  `refresh` · `social` · `me` · `me/devices`
- `GET /next-step` — **الخطوة الواحدة المقترحة** (موجودة كمنطق في `tq_home.php`)
- `GET /timeline?date=`
- `POST /lessons/{id}/notes`
- `GET /lessons/{id}/alternate`
- `GET /mistakes/drill` — مجموعة مراجعة مولدة
- كل التجارة: `catalog` · `orders` · `pay` · `invoice` · `payouts`
- كل الحصص: `slots` · `sessions` · `confirm` · `join`
- المعلم: `overview` · `students` · `submissions` · `grade` · `analytics/heatmap`
- الإدارة: `reports/{axis}` · `teachers/{id}/assignments` · `payouts/{id}/approve`
- الخصوصية (فصل 26): `GET /me/export` ✅ موجود كمسار مصير · `DELETE /me` ✅ ·
  `POST /me/consents` ⚠️ جزئي في `parent_links`

---

## 8. خط أنابيب المحتوى والفيديو

### 8.1 المخطط (فصل 08 + 12)

```
المعلم:  رفع بالقطع → إدخال الأهداف والأسئلة → إرسال للمراجعة
النظام:  فحص الصيغة → ترميز HLS بأربع درجات → استخراج نص وفهرسته
         → توليد صور مصغرة → فحص التشابه
الإدارة: مراجعة علمية → مراجعة فنية → اعتماد أو رد بسبب

حالات asset:
uploading → uploaded → processing → processed → in_review → { published | rejected }
                                  ↓
                              failed (3 محاولات بتراجع أسي)

درجات الترميز: 240p/400k · 480p/1000k · 720p/2500k · 1080p/4500k
```

**إجراءات الحماية:** روابط HLS تنتهي خلال 5 دقائق ومربوطة بالجلسة والجهاز ·
`FLAG_SECURE` على أندرويد · علامة مائية ديناميكية باسم الطالب · 3 أجهزة كحد
أقصى · جلسة مشاهدة واحدة متزامنة · تخزين مؤقت مشفر.

### 8.2 الواقع

| المرحلة | الحالة |
|---|---|
| رفع بالقطع | ❌ رفع عادي — `Taqdar_teacher_model::save_upload()` |
| فحص الصيغة والمدة | ⚠️ فحص نوع أساسي |
| ترميز HLS بأربع درجات | ❌ **غائب تماما** |
| استخراج النص وفهرسته | ❌ **غائب** — والمخطط يحذر: بناؤه لاحقا يعني إعادة معالجة المكتبة كلها |
| توليد صور مصغرة | ❌ |
| فحص التشابه | ❌ |
| مراجعة علمية + فنية | ⚠️ `lesson.tq_status` عمود موجود، **لا واجهة مراجعة نشر** |
| اعتماد أو رد بسبب | ❌ |

**آلة حالة الأصل الفعلية:** `lesson.tq_status` قيمة واحدة (`published`) في كل
الصفوف. `paths.status enum('draft','published')` هي القفل الحقيقي.

**الحماية:** ❌ **صفر**. `lesson.video_url` رابط YouTube أو رابط مباشر دائم.
لا روابط موقعة، ولا علامة مائية، ولا حد أجهزة، ولا جلسة واحدة.

> **هذه أكبر فجوة تجارية في النظام:** المحتوى المدفوع محمي بالمصادقة وحدها،
> ورابط الفيديو بمجرد استخراجه يعمل للأبد ولأي أحد.

---

## 9. المدفوعات والمحفظة

### 9.1 المخطط (فصل 14)

```
الشراء:  إنشاء طلب → تهيئة الدفع → تأكيد Webhook → فتح الوصول + فاتورة
التوزيع: حساب الصافي → قيد معلق للمعلم → تحرير بعد المهلة
السحب:   طلب → مراجعة مالية → تحويل → إيصال
```

**قواعد السلامة المالية الأربع:**
1. الاعتماد على الـwebhook لا على عودة المستخدم — المتصفح قد يغلق قبل الرجوع.
2. مفتاح تفرد لكل عملية (`idempotency key`) لمنع الشحن المزدوج.
3. قيود المحفظة مزدوجة القيد وغير قابلة للحذف — التصحيح بقيد عكسي لا بمسح.
4. كل مبلغ بعملة صريحة، **يخزن بالقروش كأعداد صحيحة** لا بأرقام عائمة.

**قواعد التسوية:** نافذة الاسترداد 14 يوما · أقل سحب 200 جنيه · دورة اعتماد
أسبوعية · تجميد عند نزاع بقيمة المعاملة فقط.

**قنوات السحب:** فودافون كاش · إنستا باي · فيزا · تحويل بنكي.

### 9.2 الواقع

| القاعدة | الحالة |
|---|---|
| اعتماد على webhook | ❌ **لا بوابة دفع مفعلة أصلا** |
| `idempotency key` | ❌ على مستوى الطلب. ✅ على مستوى القيد (`uq_we_ref` فريد) |
| قيود غير قابلة للحذف | ⚠️ عرفا لا إجبارا |
| **الهللات كأعداد صحيحة** | ✅ **مطابق** — `paths.price` · `plans.price` · `wallet_entries.amount` · `payout.amount_halalas` كلها `bigint` |
| نافذة الاسترداد 14 يوما | ✅ `taqdar_refund_window_days = 14` |
| أقل سحب | ✅ 100 ريال (`taqdar_payout_min_sar`) |
| قنوات السحب | ⚠️ `payout.requested_channel` + `destination` — **بلا تكامل تحويل** |

**آلية الشراء الفعلية:**

```
/plans → /plan/{code} → /checkout/{code}
                            │
                            ├─ غير مسجل     → /login?next=checkout/{code}
                            ├─ ليس طالبا     → رفض برسالة (المعلم وولي الأمر لا يشتريان)
                            └─ طالب          → عرض بيانات التحويل البنكي (tqs_bank_block())
                                                  ↓
                                         POST /student/subscribe
                                                  ↓
                                    subscriptions.status = 'pending'
                                                  ↓
                        الإدارة: taqdar_admin/subscription_activate/{id}  ← يدوي
                                                  ↓
                                    status = 'active' + enrol + invoice + قيد محفظة المعلم
```

`Taqdar::gateway_callback()` موجودة **لكنها ترد `show_404()` وتسجل نداءها** —
نقطة عودة محجوزة لبوابة لم تفعل بعد. بيانات التحويل البنكي تدار من
`taqdar_admin/bank`.

---

## 10. الإشعارات

### 10.1 المخطط (فصل 15) + فلو ولي الأمر (فصل 03)

**القنوات وحدودها:** دفع (2 يوميا للطالب) · داخل التطبيق (بلا حد) · بريد
(حسب الحدث) · واتساب (1 أسبوعيا) · رسالة نصية (رمز التحقق فقط).

**ساعات الصمت:** 22:00 – 07:00 بتوقيت المستخدم، وصمت تام أثناء الامتحانات
الرسمية. المعاملات المالية والحصص تتجاوزها.

**التسليم:** طابور خارج دورة الطلب · 3 محاولات بتراجع أسي ثم صف الرسائل الميتة ·
`dedupe` بمفتاح `(user_id, event, entity_id)` خلال 10 دقائق.

**الأحداث الخمسة الفورية لولي الأمر — وما عداها ينتظر التقرير الأسبوعي:**
1. نتيجة امتحان
2. رسوب في اختبار محطة
3. انقطاع 3 أيام متتالية
4. طلب حصة خاصة
5. شهادة جديدة

### 10.2 الواقع — `Taqdar_events_model.php`

**الأنواع الخمسة منفذة بالحرف** في `$catalog`:

| النوع | عنوان الطالب | عنوان ولي الأمر |
|---|---|---|
| `exam_result` | ظهرت نتيجة امتحانك | نتيجة امتحان {name} |
| `station_failed` | لم تجتز اختبار المحطة | لم يجتز {name} اختبار المحطة |
| `inactivity_3days` | انقطعت عن الدراسة ثلاثة أيام | انقطع {name} عن الدراسة ثلاثة أيام |
| `session_request` | أرسل طلب حصة خاصة | طلب حصة خاصة لـ{name} |
| `certificate` | حصلت على شهادة جديدة | شهادة جديدة لـ{name} |
| `weekly_report` | تقريرك الأسبوعي جاهز | تقريرك الأسبوعي جاهز |

**ثلاثة قرارات هندسية موثقة في رأس الملف:**

1. **الطابع الزمني** يكتب طابع يونكس **نصا** كما يكتبه السكربت الأصلي — من
   يكتب بصيغة أخرى تظهر إشعاراته «منذ 56 سنة».
2. **منع التكرار ببصمة لا بالحدس.** «نفس الحدث» ليس «نفس النوع»: نتيجتا
   امتحانين في يوم حدثان. فلكل نداء بصمة `<!--tq:HASH-->` مخبوءة في نهاية
   الوصف، تشتق من النوع ومن `key` يمرره المطلق. البصمة تعليق HTML: يمحوه
   `strip_tags`، ولا يعرضه متصفح، ولا يحتاج عمودا جديدا في جدول مشترك.
3. **البريد خلف مفتاح مطفأ.** لا يحاول الإرسال إلا إذا كان `taqdar_events_email = 1`
   **و** `smtp_user` غير فارغ — وإلا علقت المهمة الدورية عند مهلة الاتصال.

**تحذير مكتوب:** النموذج يستدعى من الكرون، فلا يقرأ `$this->session` بحال —
لا جلسة في سطر الأوامر، وقراءتها هناك تسقط المهمة بعد أن تكون قد غيرت البيانات.

### 10.3 المهام الدورية

`Taqdar_cron_events.php` — مقلع CLI يعيش **فوق** جذر الويب (`server/cron_taqdar.php`):

| الدالة | ما تفعل |
|---|---|
| `inactivity()` | انقطاع 3 أيام |
| `exams()` | نتائج الامتحانات |
| `stations()` | رسوب اختبار محطة |
| `certificates()` | شهادات جديدة |
| `session_requests()` | طلبات الحصص |
| `weekly()` | التقرير الأسبوعي |

**الجدولة الفعلية** (`server/crontab.taqda9296`، بمفسر `lsphp82` وبصلاحية مالك
الملفات لا الـroot):

```
0  2 * * *   taqdar_cron        expire            # انتهاء الاشتراكات
30 2 * * *   taqdar_cron_events run               # الأحداث اليومية
0  4 * * 0   taqdar_cron_events weekly            # التقرير الأسبوعي (الأحد)
10 * * * *   taqdar_cron        expire_sessions   # تحرير المواعيد المعلقة (كل ساعة)
40 4 * * *   taqdar-ci-logs-prune                 # تقليم السجلات
```

### 10.4 الفجوات

- ❌ **لا واتساب** — والمخطط يجعله قناة التقرير الأسبوعي الأساسية لولي الأمر.
- ❌ **لا إشعارات دفع** ولا `deep link`.
- ❌ **لا ساعات صمت** ولا استثناء للمالي.
- ❌ لا طابور حقيقي — النداء متزامن داخل دورة الطلب أو من الكرون.
- ✅ `dedupe` موجود لكن بنافذة **اليوم الجاري** لا 10 دقائق.

---

## 11. البحث والاكتشاف

### 11.1 المخطط (فصل 09)

| المصدر | الوزن | ما يعاد |
|---|---|---|
| عنوان الدرس | 3.0 | الدرس + موضعه في المسار |
| الهدف التعليمي | 2.5 | الدرس + الختم الزمني للهدف |
| نص الفيديو | 1.5 | الدرس + الثانية التي ذكر فيها |
| نص السؤال | 1.2 | السؤال + الهدف المرتبط |
| سؤال مجتمعي | 0.8 | المنشور + إجابته المعتمدة |

**التصفية الإلزامية:** `lesson.status='published'` · `grade_id IN enrolled_grades` ·
`subject_id IN entitled_subjects` (طالب) · `teacher_id = me` (معلم).
**الترتيب:** `(bm25 * source_weight) + personal_boost`، حيث `personal_boost = 0.4`
إذا كان الهدف ضمن نقاط ضعف الطالب.

**مراحل التنفيذ:** (1) Postgres FTS — كاف حتى 50 ألف وثيقة · (2) pgvector هجين ·
(3) OpenSearch عند تجاوز 200 مللي ثانية عند p95.

### 11.2 الواقع

بحثان منفصلان في `Taqdar.php`:

| الدالة | السطر | لمن |
|---|---|---|
| `site_search()` | 242 | كل زائر — بحث الموقع العام |
| `search()` | 260 | من له بوابة — يبقيه فيها |

مسارات `student/search` · `teacher/search` · `parent/search` مصرحة في
`routes.php:186-188` عمدا: بدونها كان `teacher/search` يسقط إلى
`Taqdar::teacher('search')` — قسم غير موجود — فترد 404 على زر في ترويسة كل صفحة.

**التنفيذ:** `LIKE` على العناوين. ❌ لا أوزان · ❌ لا `personal_boost` ·
❌ لا بحث في نص الفيديو (لا نص أصلا) · ❌ لا FTS ولا متجهات.

---

## 12. الذكاء الاصطناعي

**المخطط (فصل 13):** استرجاع مقيد بالمنهج (RAG) لا نموذج مفتوح — **قرار سلامة
قبل أن يكون قرار كلفة**.

```
سؤال الطالب + سياق الدرس والثانية
        ↓ تضمين
بحث في متجهات هذا الدرس ومنهجه فقط
        ↓ مقاطع
توليد الإجابة من المقاطع حصرا
        ↓ فحص
إن لم تكف المقاطع ← اعتذار وإحالة للمعلم
```

**الضوابط الخمسة الإلزامية:**
1. الإجابة تحمل مرجعا — الدرس والثانية — وإلا لا تعرض.
2. عند غياب مقاطع كافية: رسالة اعتذار ثابتة وزر «اسأل المعلم». **ممنوع التخمين.**
3. كل مخرج موسوم بأنه مولد آليا واسترشادي.
4. لا يصحح مقالي نهائيا بلا اعتماد المعلم.
5. تسجيل كل سؤال وإجابة للمراجعة ولقياس نسبة الاعتذار.

**الواقع:** ❌ **غير موجود بأي صورة.** لا تكامل نموذج، ولا قاعدة متجهات، ولا نص
فيديو يفهرس.

**لكن الضابط الرابع منفذ بمعناه:** `Taqdar_marking_model::approve()` — الدرجة
النهائية للواجب لا تظهر للطالب قبل اعتماد المعلم صراحة. النظام يقترح، والمعلم
يعتمد. الأساس الذي يبنى عليه التصحيح الآلي **جاهز**.

---

## 13. كتالوج الأخطاء

### 13.1 مغلف المخطط (فصل 10 + 17)

```json
{ "error": {
    "code": "MASTERY_LOCKED",
    "message": "Lesson is locked until the previous review is passed",
    "message_ar": "الدرس مقفل حتى تجتاز مراجعة الدرس السابق",
    "details": { "blocking_lesson_id": 842 } } }
```

### 13.2 المطابقة

الكتالوج الفعلي: `Taqdar_repo_model::$ERRORS` — مصدره الوحيد، والمغلف يطابق
المخطط تماما (`code` · `message` · `message_ar` · `details`).

| رمز المخطط | HTTP | موجود؟ | النص العربي الفعلي |
|---|---|---|---|
| `MASTERY_LOCKED` | 403 | ✅ | أكمل مراجعة الدرس السابق أولا |
| `NOT_ENTITLED` | 403 | ✅ | هذا المحتوى غير متاح ضمن اشتراكك |
| `OUT_OF_ASSIGNMENT` | 403 | ✅ | هذه المادة أو هذا الصف خارج نطاق إسنادك |
| `DUPLICATE_ATTEMPT` | 409 | ✅ | هذه المحاولة مسلمة من قبل |
| `RATE_LIMITED` | 429 | ✅ | محاولات كثيرة في وقت قصير |
| `TOKEN_EXPIRED` | 401 | ⚠️ | باسم `UNAUTHENTICATED` — يلزم تسجيل الدخول |
| `KYC_REQUIRED` | 403 | ❌ | — لا توثيق أصلا |
| `SLOT_TAKEN` | 409 | ❌ | — منع التزامن بالقاعدة لا برمز |
| `INSUFFICIENT_BALANCE` | 422 | ❌ | — رسالة في `Taqdar_wallet_model::fail()` |
| `BELOW_MINIMUM` | 422 | ❌ | — كذلك |
| `OUT_OF_SCOPE` | 422 | ❌ | — |
| `PAYMENT_FAILED` | 402 | ❌ | — لا بوابة |
| `TRANSCODE_PENDING` | 503 | ❌ | — لا ترميز |

**زيادات على المخطط:** `NOT_FOUND` (404) · `NO_REVIEW` (404) · `VALIDATION` (422) ·
`INTERNAL` (500).

**قاعدة الرسائل من المخطط:** لا تظهر رسالة خطأ لا يستطيع المستخدم فعل شيء
حيالها. «خطأ 500» بلا إجراء مقترح تترجم إلى «حدث خلل، وأعدنا المحاولة تلقائيا»
مع زر إعادة — والتفصيل يذهب إلى السجل لا إلى الشاشة. ✅ ملتزم به.

---

## 14. نظام التصميم

### 14.1 المخطط (فصل 18) مقابل `assets/taqdar/css/tokens.css`

| الرمز | قيمة المخطط | القيمة الفعلية | مطابق؟ |
|---|---|---|---|
| أساسي | `#132549` كحلي | `--tq-navy: #023331` **أخضر داكن** | ❌ |
| النجاح/الإتقان | `#0C8274` تركوازي | `--tq-teal: #0C786C` | ≈ قريب |
| التنبيه/المحطة | `#F49B2D` برتقالي | `--tq-amber: #8A5D18` | ❌ أغمق |
| أرضية | `#EAEEF5` | `--tq-ground: #F5F7F8` | ❌ |
| أنصاف الأقطار | 28 · 20 · 14 · pill | 32 · 28 · 20 · 14 · pill | ✅ + درجة |
| الخطوط | Tajawal · Cairo · Space Grotesk | **Cairo · IBM Plex Arabic · SpaceGrotesk** | ⚠️ Plex بدل Tajawal |
| الحركة | 220 · 320 · 60ms | `route:220` · `elementEntry:320` · `stagger:60` | ✅ **مطابق حرفيا** |

**تدرجات إضافية في الواقع:** `navyDeep` · `navySoft` · `tealSoft` · `tealLift` ·
`amberSoft` · `navyWash` · `navyWashStrong` · `motion-ringFill:900ms` ·
`motion-reduced:120ms` · `motion-hover:150ms`.

**سلم الطباعة الفعلي:** `hero` · `eyebrow` · `display` · `h1` · `h2` · `body` ·
`bodyStrong` · `caption` · `micro` · `numeralXl` · `numeralMd` · `numeralSm`.

> **الخلاصة:** المخطط يصف هوية كحلية-برتقالية. المنفذ هوية **خضراء-تركوازية**.
> هذا تطور علامة تجارية متعمد لا انحراف — لكنه يعني أن **جدول ألوان فصل 18 لاغ**.

### 14.2 القاعدتان اللتان لا تكسران في الواجهة العربية

من فصل 18 و21 — **ملتزم بهما بالكامل**:

1. **الاتجاه:** `start/end` حصرا. أي `left/right` في التخطيط عيب لا خيار أسلوبي.
   → الشيفرة تستعمل `padding-block` · `border-block-end` · `margin-inline`.
2. **الأرقام:** كل تتابع أرقام لاتيني داخل نص عربي يعزل اتجاهيا، والنطاق مثل
   `9:00 – 10:30` يعزل كوحدة واحدة لا كطرفين.
   → `tqa_ltr()` في `taqdar_admin_helper.php`: `<span class="tq-ltr" dir="ltr">`.
   → `tqa_money()`: **الهللات تخزن أعدادا صحيحة وتعرض ريالات — القسمة في موضع
   واحد وحده.**

**العطب الذي يتكرر (فصل 21):** خوارزمية bidi تعيد ترتيب المقاطع حول الفواصل
المحايدة لا الحروف. لذلك «١ – ٢» بمسافات ينعكس، بينما `2026-07-15` بلا مسافات
يسلم. كل رقم يظهر للمستخدم يمر عبر مكون عزل واحد — لا استثناءات.

### 14.3 قاعدة التعريب المخالفة عمدا

المخطط يوجب `l10n/ar.arb` وفحصا آليا يفشل عند وجود حرف عربي داخل `.dart` أو
`.tsx`. **الواقع: النص العربي مكتوب في القوالب مباشرة** — وهو قرار متسق مع
تصيير الخادم وغياب خطوة البناء.

**لكن قاعدة أخرى تحل محلها وتفرض بأداة:** [CLAUDE.md](CLAUDE.md) يمنع **التشكيل**
(U+064B–U+0652) في أي نص: قالبا أو سلسلة شيفرة أو صف قاعدة.
`scripts/strip_tashkeel_db.php` ينظف القاعدة (`--apply` للتنفيذ)، وهو مأمون
التكرار ويقرأ المخطط وقت التشغيل فيلتقط الجداول الجديدة وحدها.
المستثنى عمدا: `audit_log` (سجل يحفظ القيم كما كانت) و`pdf.worker.min.js`
(جداول تفكيك الأشكال العربية).

---

## 15. جرد الشاشات الكامل

### 15.1 الموقع العام (Landing + الصفحات الأساسية)

**القالب الجذر:** `application/views/frontend/taqdar/index.php` (106 أسطر) —
غلاف الثيم. `Taqdar::show($page, $title, $data)` يحمله دائما.

**قطع الموقع المشتركة** — `views/frontend/taqdar/site/`:
`site_header.php` · `site_footer.php` · `site_pagehero.php` · `site_prose.php` ·
`site_arch.php` · `site_lantern.php` · `site_sprite.php` (أيقونات SVG) ·
`site_tabbar.php` · `site_stores.php` · `site_authaside.php`.

| المسار | المتحكم | القالب | المحتوى |
|---|---|---|---|
| `/` | `Home::index` | `taqdar/home.php` | **الصفحة الرئيسية** |
| `/plans` | `Taqdar::plans` | `plans.php` | الباقات — **وجهة `/courses` بعد 301** |
| `/plan/{code}` | `Taqdar::plan_page` | `site_plan.php` | تفاصيل باقة |
| `/checkout/{code}` | `Taqdar::checkout` | `site_checkout.php` | تأكيد الاشتراك + تحويل بنكي |
| `/path/{slug}` | `Taqdar::path_page` | `site_path.php` | تفاصيل المسار |
| `/categories` | `Taqdar::categories` | `categories.php` | الأقسام |
| `/teachers` | `Taqdar::site_page` | `site_teachers.php` | المعلمون |
| `/instructor/{id}` | `Taqdar::instructor_page` | `instructor_page.php` | صفحة معلم |
| `/students` | `Taqdar::site_page` | `site_students.php` | صفحة الطلاب |
| `/parents` | `Taqdar::site_page` | `site_parents.php` | صفحة أولياء الأمور |
| `/books` | `Taqdar::site_page` | `site_books.php` | كتب المنهج |
| `/competitions` | `Taqdar::competitions` | `competitions.php` | المسابقات |
| `/search` | `Taqdar::site_search` | `site_search.php` | البحث العام |
| `/about` `/contact` `/faq` `/terms` `/privacy` `/refund` | `Home::*` | قوالب مفردة | صفحات ثابتة |
| `/blogs` `/blog/{slug}/{id}` | `Blog` | `blogs.php` · `blog_details.php` | المدونة |
| `/verify/{code}` | `Taqdar::verify` | — | **التحقق من شهادة — عامة عمدا** |

**بنية الصفحة الرئيسية** (`home.php`، 242 سطرا) — بيانات من `Taqdar_site_model`
عبر `get_instance()` لا `$this->load` (تحميل نموذج داخل عرض CI3 ينتج بترا صامتا):

1. **الهيرو** — فيديو خلفية، `src` يحقن من `site.js` (الوسم الذي يحمل `src`
   يبدأ التنزيل قبل أن يعرف عرض الشاشة) + زر إيقاف الحركة
   العنوان: «نبني العقول ونصنع **المستقبل**»
   الوصف: «منصة تعليمية سعودية على المنهج الرسمي: برامج يبنيها معلمون، وتقييم
   يقيس **الإتقان لا الحضور**، وتقارير يراها ولي الأمر أولا بأول.»
   الأزرار: `ابدأ رحلة التعلم` → `/sign_up` · `تصفح البرامج` → `/plans`
2. **شريط المميزات** — مناهج معتمدة · تعلم تفاعلي · معلمون متميزون · متابعة مستمرة
3. **لماذا تختار تقدر؟** — 5 بطاقات
4. **آراء أولياء الأمور**
5. المواد مصنفة حسب المرحلة (`?cat=`) · الكتب · المعلمون · الإحصاءات (`taqdar_stat_*`)

### 15.2 التسجيل والدخول

| المسار | القالب | ملاحظة |
|---|---|---|
| `/sign_up` | `sign_up.php` | **بطاقات اختيار الدور** (`gate-card`): طالب · معلم · ولي أمر |
| `/login` | `login.php` | يقبل `?as=` و`?next=` |
| `/forgot_password` | `forgot_password.php` | |
| `/sign_up/verification_code` | `verification_code.php` | |

**حقول التسجيل حسب الدور** — `tq_gate` حقل مخفي يضبطه اختيار البطاقة:

| الحقل | متى يظهر |
|---|---|
| `age` | دائما — والسكربت يراقبه (`guardianCheck`) |
| `guardian_email` | **إذا كان العمر < 18** — تطبيق قاعدة القاصر (فصل 26) |
| `grade_id` | للطالب |
| `message` + مرفق | للمعلم — يكتب صفا في `applications` |
| `accept_terms` | إلزامي — يكتب `users.terms_accepted_at` |

**تطبيق قاعدة القاصر (فصل 26):** «حساب دون 18 يتطلب ربطا بولي أمر موثق لتفعيل
الشراء والحصص المباشرة.» → الحقول موجودة، والربط عبر `parent_links` بموافقة
موثقة (`consent_at` مفروض بـtrigger). ⚠️ **لكن الشراء غير مقيد فعليا بوجود ربط.**

### 15.3 بوابة الطالب — 15 شاشة + المشغل

`Taqdar::student($page, $title)` → `require_role('student')` → `show()`.
القائمة الجانبية: `portal_rail.php` مجمعة في ست مجموعات.

| # | الشاشة | المسار | القالب | شاشة المخطط |
|---|---|---|---|---|
| 1 | الرئيسية | `/student` | `tq_home.php` | 3 — الرئيسية |
| 2 | دروسي | `/student/lessons` | `tq_lessons.php` | 6 — خريطة المسار |
| 3 | محتوى باقتي | `/student/bundle` | `tq_bundle.php` | — (زيادة) |
| 4 | المواد التعليمية | `/student/materials` | `tq_materials.php` | 13 — الكتب |
| 5 | المفضلة | `/student/favourites` | `tq_favourites.php` | — |
| 6 | المراجعة | `/student/reviews` | `tq_reviews.php` | 9 — مراجعة اليوم |
| 7 | مهامي | `/student/tasks` | `tq_tasks.php` | — (الواجبات) |
| 8 | اختباراتي | `/student/exams` | `tq_exams.php` | 11 — الاختبار |
| 9 | المتابعة والتقارير | `/student/reports` | `tq_reports.php` | 17 — تقاريري |
| 10 | التقويم | `/student/calendar` | `tq_calendar.php` | 15 — الخط الزمني |
| 11 | الشهادات | `/student/certificates` | `tq_certificates.php` | — |
| 12 | حصص بالطلب | `/student/on-demand` | `tq_on_demand.php` | 16 — الحصص الخاصة |
| 13 | رسائلي | `/student/messages` | `tq_messages.php` | — |
| 14 | الإشعارات | `/student/notifications` | `tq_notifications.php` | — |
| 15 | اشتراكي | `/student/subscription` | `tq_subscription.php` | 14 — الباقات |
| 16 | المدفوعات | `/student/payments` | `tq_payments.php` | 18 — المدفوعات |
| 17 | الإعدادات | `/student/settings` | `tq_settings.php` | 19 — الملف الشخصي |
| — | **مشغل الدرس** | `/student/lesson/{course}/{lesson}` | `tq_lesson.php` | 7 — مشغل الدرس |

**شاشات المخطط الغائبة:** 1 (الافتتاح والتشخيص — **8 أسئلة تكيفية**) ·
10 (دفتر الأخطاء — **المنطق والـendpoint موجودان، لا شاشة**) ·
12 (وضع الامتحان) · 4 (الاكتشاف — تحل محله `/plans`).

**سلوك مشغل الدرس (`Taqdar::lesson()` — السطر 170):**
لا يستعلم عن الدرس في المتحكم؛ **الصفحة تطلبه من `taqdar_gate`** الذي يحسم
القفل في الخادم — «فلو بني القفل هنا لأمكن تجاوزه بتعديل الواجهة».
كورس بلا رقم درس يفتح على **موضع التوقف** (`watch_histories.watching_lesson_id`)،
مع التحقق أن الدرس المحفوظ من هذا الكورس فعلا، وإلا فأول درس غير `quiz`.

**شارات القائمة (`counts()` — السطر 83):** المفاتيح تطابق **مفاتيح بنود القائمة**
لا أسماء الجداول — بند إشعارات ولي الأمر مفتاحه `alerts` لأن مساره `parent/alerts`.
ولا تحسب أعداد دور لدور آخر: `reviews` و`tasks` استعلاما طالب، وتشغيلهما لغيره
عمل ضائع في كل صفحة.

### 15.4 بوابة المعلم — 11 قسما

`Taqdar::teacher($section)` — السطر 732.

| القسم | المسار | القالب | شاشة المخطط |
|---|---|---|---|
| اللوحة | `/teacher` | `tq_teacher_dashboard.php` | نظرة عامة + يحتاج انتباهك |
| كورساتي | `/teacher/courses` | `tq_teacher_courses.php` | المحتوى |
| رفع الدروس | `/teacher/upload` | `tq_teacher_upload.php` | محرر الدرس |
| بنك الأسئلة | `/teacher/questions` | `tq_teacher_questions.php` | بنك الأسئلة |
| الواجبات والتصحيح | `/teacher/marking` | `tq_teacher_marking.php` | التصحيح |
| طلابي | `/teacher/students` | `tq_teacher_students.php` | طلابي |
| الحصص | `/teacher/sessions` | `tq_teacher_sessions.php` | مواعيدي وحصصي |
| المحفظة والأرباح | `/teacher/wallet` | `tq_teacher_wallet.php` | المحفظة + السحب |
| الرسائل | `/teacher/messages` | `tq_teacher_messages.php` | المحادثة |
| الإشعارات | `/teacher/notifications` | `tq_teacher_notifications.php` | — |
| الإعدادات | `/teacher/settings` | `tq_teacher_settings.php` | — |

**الثلاثة الأخيرة كانت غائبة عن البوابة كلها:** لا رسائل يقرأ فيها المعلم رد
طالبه، ولا إشعارات رغم أن `counts()` يعدها له، ولا إعدادات — ولا تسجيل خروج
معها، **فلم يكن للمعلم باب يخرج منه**.

**الغائب:** ❌ مولد الاختبارات · ❌ **التحليلات والخريطة الحرارية**
(`teacher/analytics/heatmap` — تاسك `B3.9`).

**مسارات كتابة المعلم** (POST حصرا، معلنة قبل مسارات العرض):

```
teacher/upload/save       → Taqdar::upload_save()        (972)
teacher/courses/save      → Taqdar::courses_save()       (337)
teacher/marking/approve   → Taqdar::marking_approve()    (1027)
teacher/marking/homework  → Taqdar::marking_homework()   (1112)
teacher/sessions/save     → Taqdar::sessions_save()      (1226)
teacher/sessions/decide   → Taqdar::sessions_decide()    (1282)
teacher/wallet/withdraw   → Taqdar::wallet_withdraw()    (1348)
teacher/wallet/cancel     → Taqdar::wallet_cancel()      (1421)
teacher/questions/import  → Taqdar::questions_import()   (1453)
teacher/settings/save     → Taqdar::teacher_settings_save() (492)
teacher/students/message  → Taqdar::students_message()   (517)
```

### 15.5 بوابة ولي الأمر — 8 أقسام

`Taqdar::parent_portal($section)` — السطر 763.

| القسم | المسار | القالب |
|---|---|---|
| أبنائي | `/parent` | `tq_parent_children.php` |
| تفاصيل الابن | `/parent/child` | `tq_parent_child.php` |
| التقارير | `/parent/reports` | `tq_parent_reports.php` |
| التقرير الأسبوعي | `/parent/weekly` | `tq_parent_weekly.php` |
| المدفوعات | `/parent/payments` | `tq_parent_payments.php` |
| الرسائل | `/parent/messages` | `tq_parent_messages.php` |
| الإشعارات | `/parent/alerts` | `tq_parent_alerts.php` |
| الإعدادات | `/parent/settings` | `tq_parent_settings.php` |

**مسارات الكتابة:** `parent/children/link` · `parent/messages/compose` ·
`parent/settings/save`.

**دورة الربط الكاملة** — `Taqdar_parent_model.php`:

```
request_link()      ولي الأمر يطلب الربط بمعرف الابن
      ↓
grant_consent()     الطالب يوافق → status='active' + consent_at
      ↓  أو
reject_request()    الطالب يرفض
      ↓  لاحقا
withdraw_consent()  الطالب يسحب موافقته
revoke_link()       ولي الأمر يفك الرابط
cancel_request()    ولي الأمر يلغي طلبه قبل الرد
```

الطالب يستجيب من `POST /student/parent-link` → `Taqdar::parent_link_respond()`.
و`trg_parent_links_consent_bi/bu` يمنعان `status='active'` بلا `consent_at`
**على مستوى القاعدة** — الموافقة ليست عمودا اختياريا.

**قاعدة الحماية (فلو ولي الأمر، فصل 03):** ولي الأمر يرى الدرجات والانتظام
والواجبات والامتحانات والشهادات. **ولا يرى أبدا:** محادثات المساعد الذكي ·
منشورات المجتمع · كل إجابة خاطئة على حدة · **ترتيب ابنه مقابل طلاب آخرين**.

> «لا ترتيب ولا مقارنة بطلاب آخرين. مقارنة الأبناء ببعضهم أسرع طريق لتحويل
> المنصة لمصدر ضغط منزلي.»

✅ ملتزم: لا لوحة صدارة في أي شاشة. `may_message()` يقيد من يراسل من.

### 15.6 لوحة الإدارة

**قسم «منصة تقدر» في `views/backend/admin/navigation.php`** → `Taqdar_admin.php`:

| المجموعة | الوحدات |
|---|---|
| المحتوى | المواد الدراسية · الصفوف · المسارات · المحطات · استيراد المنهج |
| التقييم | الأهداف التعليمية · **ربط الأسئلة بالأهداف** · التقييمات · أرقام الموقع · بيانات التحويل البنكي · طلبات المعلمين · المسابقات |
| الأدوار | **إسناد المعلمين** · روابط أولياء الأمور |
| المال | الباقات · الاشتراكات · الفواتير · المحافظ · قيود المحافظ · البريد الصادر · **سجل التدقيق** |

**آلية CRUD عامة:** `module($key)` · `form($key,$id)` · `save()` · `delete()`
تقرأ وصفا معلنا في `Taqdar_admin_model` (جدول + حقول + أنواع)، ويصوغه
`taqdar_admin_helper.php` (`tqa_cell` بأنواع: `bool` · `money` · `seconds` ·
`number` · `datetime` · `enum` · `lines` · `ref`).

**وحدات مخصصة:** `subscriptions()` + `subscription_activate/cancel` ·
`mail()` + `mail_save/test/toggle_events` · `import()` + `import_preview/commit/template` ·
`bindings()` + `bind/bind_save` · `stats()` · `teachers()` + `teacher_review()` ·
`bank()` + `bank_save()` · `subscriptions_repair()`.

**مقابل الوحدات الإحدى عشرة في فصل 19:**

| وحدة المخطط | الحالة |
|---|---|
| الطلاب (بحث · تقدم · إعادة تعيين · إيقاف · دمج) | ⚠️ عبر `Admin::users` الأصلية — **لا دمج حسابين** |
| المعلمون (قبول · **إسناد مادة وصف** · عمولة · إيقاف) | ✅ `teachers()` + `module/teacher_assignments` |
| المحتوى (مراجعة · اعتماد · رد بسبب · إلغاء نشر) | ❌ **لا قائمة مراجعة نشر** |
| بنك الأسئلة (استيراد CSV · وسم الأهداف · أرشفة) | ✅ `import()` + `bindings()` |
| الطلبات (بحث · استرداد · إعادة إرسال فاتورة) | ⚠️ `subscriptions()` — **لا استرداد** |
| السحوبات (صف الاعتماد · جماعي · رفض · تصدير) | ⚠️ عبر `Admin::instructor_payout` |
| الحصص (القادمة · تسوية غياب · استرداد يدوي) | ❌ |
| البلاغات | ❌ |
| التسعير (الباقات · أكواد الخصم · التجربة) | ✅ `module/plans` + `coupons` |
| التقارير الستة | ⚠️ `stats()` أرقام معروضة فقط |
| سجل التدقيق (قراءة · بحث بالفاعل والكيان والمدى) | ✅ `module/audit_log` |

**إسناد المعلم — الوحدة الأهم (فصل 19):** الأثر المطلوب منفذ:
لوحة المعلم تعرض هذه المادة وهذا الصف فقط → كل استعلام في بوابة المعلم يقيد
بـ`WHERE (subject_id, grade_id) IN assignments` → سحب الإسناد لا يحذف المحتوى
المنشور بل يمنع النشر الجديد فيه.

---

## 16. فلو المستخدمين الثلاثة

### 16.1 الطالب (فصل 01 من وثيقة الفلو)

```
تسجيل/دخول
    ↓
اختيار المرحلة والمواد والهدف اليومي        ⚠️ المرحلة فقط (grade_id) — لا هدف يومي
    ↓
اختبار تشخيصي تكيفي — 8 أسئلة              ❌ غائب تماما
    ↓
لوحة القيادة — نقطة الرجوع الدائمة          ✅ /student
    ↓
╔══ حلقة الدرس (تتكرر يوميا) ══════════════════════╗
║  أهداف الدرس → الفيديو → ملاحظات تفاعلية        ║   ⚠️ لا ملاحظات موقوتة
║           → مراجعة سريعة (5 أسئلة)              ║   ✅ mastery_review_questions=5
╚══════════════════════════════════════════════════╝
    ↓
النتيجة:
  نجح (≥ pass_mark) → الدرس التالي يفتح فورا + يدخل جدول المراجعة    ✅
  فشل — محاولة 1   → إعادة توجيه لأضعف نقطة، بلا إجابة جاهزة         ✅
  فشل — محاولة 2   → شرح بديل لنفس المفهوم بأسلوب مختلف              ✅
  فشل — محاولة 3+  → اقتراح حصة خاصة، والتالي يبقى مقفولا على الخادم  ✅
    ↓
بعدها في نفس اليوم:
  مراجعة اليوم (10 أسئلة متباعدة)                                    ✅
  دفتر الأخطاء يتحدث تلقائيا مع كل إجابة خاطئة                       ✅ منطقا، ❌ شاشة
  اختبار المحطة عند نهاية كل محطة ← يفتح المحطة التالية              ⚠️ assessments موجودة
  قبل الامتحان: وضع الامتحان يحول الواجهة لخطة مراجعة               ❌
  الامتحان النهائي ← الشهادة + تحديث ملف الطالب                      ✅ certificate/verify
```

### 16.2 المعلم (فصل 02)

```
تقديم طلب انضمام (بيانات + عينة شرح)     ✅ applications + مرفق
    ↓
مراجعة لجنة + توثيق الهوية والمؤهل        ⚠️ taqdar_admin/teachers — لا توثيق رسمي
    ↓
الإدارة تسند مادة + صف                    ✅ teacher_assignments — يقفل النطاق بالكامل
    ↓
╔══ دورة إنتاج المحتوى (لكل درس) ═════════════════════════╗
║ 1. رفع ملف الدرس            [المعلم]   ✅ upload_save() ║
║ 2. توليد آلي: ملخص + خريطة                              ║
║    مفاهيم + بطاقات + أسئلة   [النظام]  ❌ غائب          ║
║ 3. مراجعة واعتماد كل مخرج    [المعلم]  ❌               ║
║ 4. إرسال للمراجعة            [الإدارة] ❌               ║
║ 5. اعتماد أو رفض بسبب ← نشر  [الإدارة] ❌               ║
╚══════════════════════════════════════════════════════════╝
    ↓
المتابعة اليومية:
  «يحتاج انتباهك» — المتعثرون مرتبون بالأولوية       ⚠️ اللوحة موجودة، الترتيب؟
  تصحيح الواجب: النظام يقترح، المعلم يعتمد فقط        ✅ approve() — الأساس جاهز
  الحصص: نشر مواعيد ← قبول حجز ← إجراء الحصة          ✅ sessions_save/decide
    ↓
المحفظة: رصيد معلق ──بعد مهلة الاسترداد──> رصيد متاح   ✅ منفذ
    ↓
طلب سحب ← اعتماد ← تحويل                              ⚠️ التحويل يدوي
```

**القاعدة الصارمة:** «الدرجة النهائية للواجب المقالي **لا تظهر للطالب قبل اعتماد
المعلم صراحة**. التصحيح الآلي مساعد لا بديل.» ✅ ملتزم.

### 16.3 ولي الأمر (فصل 03)

```
ربط الحساب بابن موجود — رابط دعوة أو رمز      ✅ request_link()
    ↓ موافقة صريحة موثقة                        ✅ grant_consent() + trigger
لوحة ولي الأمر — 5 مؤشرات فقط                  ⚠️ الشاشة موجودة، الخمسة بالضبط؟
    ↓
التقرير الأسبوعي — رسالة واحدة تلقائية عبر واتساب   ⚠️ الشاشة ✅ · الكرون ✅ · واتساب ❌
```

**المؤشرات الخمسة المطلوبة:** الدرجات عبر المواد · الانتظام (أيام الدراسة
الفعلية) · الواجبات المسلمة · نتائج الامتحانات · الشهادات المكتسبة.

**قاعدة التقرير:** «الرسالة **4 أسطر بالضبط**، لا جدول أرقام» (تاسك `B4.3`).

**فلسفة الفلو:** «طرف لا يستخدم المنصة يوميا — لذلك **التقرير هو الذي يذهب
إليه، لا العكس**.» → `Taqdar_cron_events::weekly()` كل أحد 4 صباحا. ✅

---

## 17. السبرنتات الأربعة: ما أنجز وما بقي

### 17.1 لماذا هذا الترتيب (فصل 04)

| السبرنت | التركيز | لماذا هنا |
|---|---|---|
| 1 | البنية + رحلة الطالب: الدخول والدرس | لازم يبقى فيه طالب وحساب ودرس قبل أي حاجة |
| 2 | الإتقان والتقييم والتثبيت | **بوابة الإتقان قلب المنتج** — لازم تخلص قبل ما تبني حواليها |
| 3 | رحلة المعلم كاملة | المعلم مصدر المحتوى والتصحيح اللي الطالب بيستخدمه |
| 4 | ولي الأمر + التكامل الشامل | ولي الأمر بيقرأ بيانات لازم تكون موجودة فعلا من قبل |

### 17.2 السبرنت 1 — البنية + بداية رحلة الطالب

**الهدف:** طالب يقدر يسجل، يعمل تشخيص، ويشوف لوحة قيادة حقيقية متصلة بالباك إند.

| # | التاسك | الحالة | ملاحظة |
|---|---|---|---|
| B1.1 | مشروع Laravel 12 + 3 بيئات + CI/CD | ❌ | CI3، بيئتان، **لا CI/CD** — `deploy.sh` أمر واحد |
| B1.2 | جداول users/students/teachers/assignments/subjects/grades/parent_links | ⚠️ | كلها إلا `students` و`teachers` كجدولين |
| B1.3 | RBAC (4 أدوار) + النطاق في طبقة الاستعلام | ✅ | `taqdar_role_helper` + `teacher_assignments` |
| B1.4 | Endpoints المصادقة + JWT 15د + refresh دوار | ❌ | جلسة CI3 |
| B1.5 | جداول المحتوى بالعلاقة الهرمية كاملة | ✅ | `paths→milestones→lesson→objectives` |
| B1.6 | **اختبار تشخيصي تكيفي — 8 أسئلة** | ❌ | **غائب تماما** |
| B1.7 | Endpoint لوحة القيادة: `timeline` / `next-step` | ⚠️ | منطق في `tq_home.php`، لا نقطة JSON |
| B1.8 | كتالوج الأخطاء الموحد | ✅ | `$ERRORS` |
| B1.9 | سجل تدقيق — صلاحيات DB تمنع UPDATE/DELETE فعليا | ⚠️ | الجدول ✅، **الصلاحيات ❌** |
| F1.1 | شاشة تسجيل/دخول (رقم/بريد + OTP + Google/Apple) | ⚠️ | بريد + كلمة مرور فقط |
| F1.2 | شاشة اختيار المرحلة/المواد/الهدف اليومي | ⚠️ | `grade_id` في التسجيل، لا هدف يومي |
| F1.3 | شاشة الاختبار التشخيصي — «نقطة البداية» لا درجة | ❌ | |
| F1.4 | لوحة القيادة: الخطوة التالية + حلقة اليوم + مساراتي | ✅ | `tq_home.php` |
| F1.5 | نظام التعريب: عزل الأرقام + RTL + ملف ترجمة منفصل | ⚠️ | العزل ✅ · RTL ✅ · **ملف الترجمة ❌ عمدا** |

**معيار القبول:** «طالب جديد يسجل ← يعمل تشخيص ← يوصل للوحة القيادة وكل
البيانات حقيقية من الـAPI، بلا أي بيانات وهمية.»
→ **الشق الأوسط (التشخيص) مفقود.** الباقي محقق.

### 17.3 السبرنت 2 — الإتقان والتقييم والتثبيت

**الهدف:** بوابة الإتقان تشتغل بالحرف الواحد.

| # | التاسك | الحالة |
|---|---|---|
| B2.1 | آلة حالة الفيديو + القفل على الخادم (403) | ⚠️ **القفل ✅ حرفيا** · آلة حالة الفيديو ❌ |
| B2.2 | جداول التقييم + **كل سؤال مربوط إلزاميا بـ`objective_id`** | ⚠️ العمود ✅، **`NULL` مسموح** |
| B2.3 | **قاعدة بوابة الإتقان الحرفية** | ✅ **مطابق حرفيا** |
| B2.4 | `assessments/{id}/submit` مع `next_action` | ⚠️ `review_submit` يرجع القرار بأسماء أخرى |
| B2.5 | `review_queue` + خوارزمية المراجعة المتباعدة | ✅ **مطابق حرفيا** |
| B2.6 | `review/due` + `review/answer` — دفعة 10 مرتبة | ✅ |
| B2.7 | `lesson_progress` + `skill_state` (توأم رقمي) | ✅ |
| B2.8 | دفتر الأخطاء: تجميع تلقائي + endpoints | ✅ `mistakes` — **بلا `drill`** |
| B2.9 | مولد الاختبار: نسخ متكافئة (أ·ب·ج·د) | ❌ |
| F2.1 | مشغل الدرس: نص قابل للبحث + ملاحظات موقوتة + سرعة | ⚠️ المشغل ✅، الأدوات ❌ |
| F2.2 | شاشة مراجعة الدرس + **شاشات القرار الثلاث** | ✅ |
| F2.3 | شاشة مراجعة اليوم | ✅ |
| F2.4 | شاشة دفتر الأخطاء + وضع مراجعة مركزة | ❌ |
| F2.5 | شاشة الاختبار الموقوت + وضع الامتحان | ⚠️ `tq_exams` ✅ · وضع الامتحان ❌ |
| F2.6 | التلعيب: السلسلة + حلقة الهدف — بلا لوحة صدارة | ❌ **لا `streak_days` أصلا** |

**معيار القبول:** «تنفيذ آلي لكل حالات الاختبار الحرجة المتعلقة بالطالب.»
→ **السلوك محقق، والاختبار الآلي غائب.**

### 17.4 السبرنت 3 — رحلة المعلم الكاملة

| # | التاسك | الحالة |
|---|---|---|
| B3.1 | مسار انضمام المعلم + قفل نطاق الإسناد | ✅ |
| B3.2 | رفع بالقطع + طابور ترميز HLS رباعي | ❌ |
| B3.3 | حماية الفيديو: روابط 5 دقائق + علامة مائية | ❌ |
| B3.4 | `teacher/overview` · `students` · `submissions` | ⚠️ شاشات لا نقاط |
| B3.5 | تصحيح الواجب + endpoint اعتماد الدرجة | ✅ `marking_approve` · `marking_homework` |
| B3.6 | **جداول المحفظة بقيد مزدوج فقط — لا حقل رصيد** | ⚠️ الدفتر ✅، **أعمدة الرصيد موجودة** |
| B3.7 | قواعد المحفظة: `order.paid` → معلق → متاح | ✅ ⚠️ التعليق = النافذة بلا +يومين |
| B3.8 | `wallet/payouts` — خصم عند الطلب لا الاعتماد | ✅ |
| B3.9 | `teacher/analytics/heatmap` | ❌ |
| B3.10 | `sessions` + `availability_slots` كاملة | ✅ |
| F3.1 | لوحة المعلم: «يحتاج انتباهك» في الأعلى دائما | ⚠️ |
| F3.2 | استوديو الرفع + توليد آلي + اعتماد كل مخرج | ❌ |
| F3.3 | بنك الأسئلة + مولد الاختبارات — **لا سؤال بلا `objective_id`** | ⚠️ الشاشة ✅، الإلزام ❌ |
| F3.4 | شاشة التصحيح: طابور + اقتراح درجة + اعتماد | ✅ |
| F3.5 | شاشة المحفظة: معلق/متاح + سحب + فواتير | ✅ |
| F3.6 | إدارة الحصص: نشر + قبول/رفض | ✅ |
| F3.7 | لوحة الإدارة (نسخة أولى) — كل إجراء في سجل التدقيق | ✅ |

**معيار القبول:** «معلم يرفع درس ← `published` ← طالب يشتريه ← الرصيد معلقا
← بعد المهلة متاحا ← يطلب سحب وتصل الحالة لاعتماد الإدارة.»
→ **الدورة كاملة تعمل** — عدا أن الشراء بتحويل بنكي يفعله الأدمن يدويا.

### 17.5 السبرنت 4 — ولي الأمر والتكامل الشامل

| # | التاسك | الحالة |
|---|---|---|
| B4.1 | `parent/children` + فرض `parent_links` النشط فقط (403) | ✅ |
| B4.2 | حساب المؤشرات الخمسة | ⚠️ |
| B4.3 | `weekly-report` + محرك رسالة بشرية — **4 أسطر بالضبط** | ⚠️ |
| B4.4 | طابور إشعارات (retry + dedupe + ساعات صمت) | ⚠️ `dedupe` ✅ · **الطابور والصمت ❌** |
| B4.5 | الإشعارات الفورية الخمسة فقط | ✅ **مطابق حرفيا** |
| B4.6 | Endpoint دفع نيابة عن الطالب — إنشاء فقط | ❌ |
| B4.7 | الشهادات: توليد + **رمز تحقق QR** | ✅ `phpqrcode` + `/verify/{code}` |
| B4.8 | لوحة الإدارة الكاملة: تقارير + صلاحيات + تدقيق | ⚠️ **الصلاحيات ❌** |
| B4.9 | اختبار تكامل شامل E2E | ❌ |
| F4.1 | لوحة ولي الأمر — لا محادثات ذكاء ولا مجتمع | ✅ |
| F4.2 | شاشة التقرير الأسبوعي — نفس المحتوى في القناتين | ⚠️ قناة واحدة |
| F4.3 | الدفع نيابة عن الابن + سجل مدفوعات | ⚠️ السجل ✅، الدفع ❌ |
| F4.4 | ملف الطالب النهائي: خريطة الإتقان + الشهادات | ⚠️ `skill_map` ✅ كنقطة، الشاشة؟ |
| F4.5 | لوحة الإدارة: الوحدات المتبقية | ⚠️ |
| F4.6 | مراجعة تعريب شاملة + فحص آلي | ⚠️ فحص التشكيل بدلها |

---

## 18. حالات الاختبار الحرجة الثمانية عشرة

من فصل 22. «الفشل في أي منها يعني أن المنتج لا يعمل، لا أن فيه عيبا تجميليا.»

| # | الحالة | النتيجة المتوقعة | الحالة | أين |
|---|---|---|---|---|
| 1 | طالب يطلب درسا لم يجتز مراجعة سابقه | 403 بلا رابط تشغيل | ✅ | `Taqdar_gate::lesson()` |
| 2 | يرسب أول مرة | إعادة + توقيت المفهوم الأضعف | ✅ | `gate_decision()` |
| 3 | يرسب ثالث مرة | اقتراح حصة مع تمرير المفهوم | ✅ | `gate_decision()` |
| 4 | إجابة صحيحة في المراجعة | يتباعد الموعد ولا يتجاوز 60 يوما | ✅ | `answer_review()` |
| 5 | إجابة خاطئة | تدخل دفتر الأخطاء ويعود السؤال غدا | ✅ | `answer_review()` + `get_mistakes()` |
| 6 | سحب يتجاوز الرصيد المتاح | رفض مع رسالة واضحة | ✅ | `request_payout()` |
| 7 | استرداد قبل تحرير الرصيد | عكس القيد ولا رصيد سالب | ✅ | `reverse_origin()` |
| 8 | سحب لمعلم غير موثق | رفض | ❌ | **لا توثيق أصلا** |
| 9 | وصول webhook مرتين لنفس العملية | لا شحن مزدوج ولا فتح مزدوج | ❌ | **لا webhook** |
| 10 | انتهاء مهلة حجز الموعد بلا دفع | يعود الموعد متاحا | ⚠️ | `expire_sessions()` — بلا دفع |
| 11 | إلغاء الطالب قبل ساعة | لا استرداد | ❌ | لا مال في الحصص |
| 12 | معلم يطلب طالبا خارج إسناده | 403 | ✅ | `teacher_owns_course()` |
| 13 | ولي أمر يطلب بيانات طفل غير مرتبط | 403 | ✅ | `parent_owns_child()` |
| 14 | محاولة تشغيل برابط منته | رفض وتجديد عبر الخادم | ❌ | **الروابط دائمة** |
| 15 | جهاز رابع يحاول التشغيل | رفض مع عرض إدارة الأجهزة | ❌ | **لا حد أجهزة** |
| 16 | سؤال للذكاء خارج المنهج | اعتذار وإحالة — لا تخمين | ❌ | لا ذكاء |
| 17 | إجابات أرسلت دون اتصال | ترفع وتدمج بلا تكرار | ⚠️ | `uq_answer_attempt_question` يحمي |
| 18 | عرض نطاق زمني في واجهة عربية | `9:00 – 10:30` بالترتيب الصحيح | ✅ | `tqa_ltr()` |

**المحصلة: 9 محققة ✅ · 3 جزئية ⚠️ · 6 محجوبة ❌ بغياب أنظمة كاملة
(الدفع · حماية الفيديو · الذكاء · توثيق المعلم).**

**ولا واحدة منها مغطاة باختبار آلي.** التحقق بالتصفح المحلي ثم على الإنتاج.

---

## 19. الاختلافات المتعمدة عن المخطط

هذه **ليست فجوات** — قرارات مختلفة عن الوثيقة، ومعرفتها يمنع «تصحيحا» خاطئا:

### 19.1 السوق: السعودية لا مصر

| البعد | المخطط | الواقع |
|---|---|---|
| العملة | جنيه مصري / قروش | **ريال سعودي / هللات** |
| المنطقة الزمنية | — | **`Asia/Riyadh`** (بديل صريح إن كان `settings.timezone` غير صالح) |
| البوابة | Paymob | **تحويل بنكي** (وبوابة محجوزة) |
| قنوات السحب | فودافون كاش · إنستا باي | **تحويل بنكي (IBAN) · محفظة** |
| أقل سحب | 200 جنيه | **100 ريال** |
| المنهج | مصري | **سعودي** — `grades` بمراحله، `books` بكتبه |
| العنوان في `settings` | — | «المملكة العربية السعودية» |

### 19.2 المعمارية: تصيير من الخادم لا SPA

- لا Flutter، لا Next.js، لا npm، لا خطوة بناء.
- الجلسة كوكي CI3 لا JWT.
- **مسارات `/student/*` · `/teacher/*` · `/parent/*` صممت عمدا لتطابق مسارات
  تطبيق Flutter المستقبلي** — الجسر مبني حتى لو لم يعبره أحد بعد.
- `Taqdar_repo_model` **يصف نفسه بأنه «الطبقة الانتقالية»**: كل دالة تطابق عقد
  `/api/v1` المستقبلي اسما ومخرجا، «فلا تتغير الشاشات حين تهاجر الطبقة إلى
  Laravel/PostgreSQL: يتبدل ما تحت الدالة لا ما فوقها».

### 19.3 النموذج التجاري: اشتراك لا طلب

المخطط يفترض `orders` بـ`item_type: path|book|bundle|session`. الواقع
`subscriptions` + `plans` بنطاق (`scope: all|subject|path|trial|grade` +
`scope_ids`). الباقات مرحلية: `basic/plus/full` × `primary/middle/secondary`.

### 19.4 الهوية البصرية: خضراء لا كحلية

انظر [14.1](#141-المخطط-فصل-18-مقابل-assetstaqdarcsstokenscss). جدول ألوان فصل 18 لاغ.

### 19.5 التعريب: نص في القالب + منع التشكيل

المخطط يوجب `l10n/*.arb` وفحصا يفشل عند نص عربي في الشيفرة. الواقع يكتب النص
في القالب، **ويفرض بدلها قاعدة أشد وأنفع لهذا السياق: لا تشكيل في أي نص** —
مع أداة تنظف القاعدة وتقرأ المخطط وقت التشغيل.

### 19.6 زيادات على المخطط

- **المسابقات** (`competitions` · `competition_entries`) — غير مذكورة أصلا.
- **`balance_locked`** ثالث دلاء المحفظة — يحل مشكلة يذكرها فصل 07 ولا يعطي لها بنية.
- **`wallet_entries.subject`** — «وصف السطر لحظة القيد، لا يتغير بتغير الكورس».
- **`wallet_entries.occurred_at`** — «وقت الحدث لا وقت الكتابة».
- **`tutoring_sessions.meet_url`** — Google Meet بدل LiveKit.
- **بصمة منع التكرار كتعليق HTML** — حل ذكي بلا عمود جديد في جدول مشترك.
- **`audit_log` مستثنى من تجريد التشكيل** — «سجل يحفظ القيم كما كانت، وإعادة
  كتابته تزوير».

---

## 20. الفجوات مرتبة بالأولوية

### 🔴 حرجة — تمنع التشغيل التجاري

| # | الفجوة | الأثر | المرجع |
|---|---|---|---|
| 1 | **لا بوابة دفع مفعلة** | كل اشتراك يفعله أدمن يدويا بعد تحويل بنكي. لا نمو | فصل 14 |
| 2 | **لا حماية للفيديو** | رابط YouTube/مباشر دائم — المحتوى المدفوع مكشوف بمجرد استخراج الرابط | فصل 12 |
| 3 | **لا webhook ولا idempotency** | ضغطة دفع مزدوجة = خصم مزدوج. القاعدة رقم 2 من قواعد السبرنتات الثابتة | فصل 10 · 14 |
| 4 | **لا توثيق معلم (KYC)** | سحب لمعلم غير موثق يمر. حالة الاختبار 8 | فصل 07 |

### 🟠 عالية — تنقص جوهر المنتج

| # | الفجوة | الأثر | المرجع |
|---|---|---|---|
| 5 | **الاختبار التشخيصي (8 أسئلة)** | أول شاشة في فلو الطالب، ونقطة البداية التي يبنى عليها المسار | `B1.6` · `F1.3` |
| 6 | **`objectives.at_second` كلها صفر** | `seek_to` يعيد الطالب لبداية الفيديو لا لموضع ضعفه — **يعطل نصف قيمة بوابة الإتقان** | فصل 06 |
| 7 | **`question.objective_id` يقبل NULL** | سؤال بلا هدف لا يغذي المراجعة ولا التوأم. `F3.3` يقول: «لا سؤال يحفظ بدون `objective_id`» | `B2.2` |
| 8 | **لا شاشة دفتر أخطاء** | المنطق والـendpoint جاهزان، والطالب لا يراهما | شاشة 10 · `F2.4` |
| 9 | **لا واتساب** | التقرير الأسبوعي هو **كل** علاقة ولي الأمر بالمنصة، والقناة المصممة له غائبة | فصل 15 |
| 10 | **لا مراجعة نشر محتوى** | المعلم ينشر بلا مراجعة علمية أو فنية | فصل 08 · 19 |
| 11 | **لا اختبارات آلية** | 18 حالة حرجة، صفر اختبار. القاعدة رقم 4 | فصل 22 |

### 🟡 متوسطة — تكمل التجربة

| # | الفجوة | المرجع |
|---|---|---|
| 12 | لا تلعيب: `streak_days` · `daily_goal_minutes` غير موجودين أصلا | `F2.6` |
| 13 | لا وضع امتحان | شاشة 12 · `F2.5` |
| 14 | لا خريطة حرارية للمعلم | `B3.9` |
| 15 | لا مولد اختبارات بنسخ متكافئة | `B2.9` · `F3.3` |
| 16 | لا ملاحظات موقوتة ولا بحث في نص الفيديو | `F2.1` |
| 17 | لا صلاحيات دقيقة في لوحة الإدارة (5 أدوار فرعية) | فصل 19 |
| 18 | لا ساعات صمت ولا طابور إشعارات حقيقي | فصل 15 · `B4.4` |
| 19 | لا `Idempotency-Key`، لا ترقيم بالمؤشر، لا `can:{}` في الاستجابات | فصل 10 |
| 20 | `audit_log` غير محمي بصلاحيات DB | `B1.9` |

### 🔵 مؤجلة بحكم المرحلة

| # | البند | لماذا مؤجل |
|---|---|---|
| 21 | الذكاء الاصطناعي (RAG) | يعتمد على نص الفيديو المفهرس — وهو نفسه غائب |
| 22 | بحث دلالي / FTS بأوزان | `LIKE` يكفي عند هذا الحجم (فصل 09 يقول: FTS كاف حتى 50 ألف وثيقة) |
| 23 | تطبيق Flutter | المسارات مهيأة له |
| 24 | الهجرة إلى Laravel/PostgreSQL | `Taqdar_repo_model` مصمم ليجعلها ممكنة بلا لمس الشاشات |
| 25 | CI/CD + ثلاث بيئات | `deploy.sh` يفحص الموقع فعليا ويفشل إن لم يرجع 200 |

---

## 21. خريطة الملفات السريعة

### المتحكمات

| الملف | الأسطر | ما فيه |
|---|---|---|
| [Taqdar.php](application/controllers/Taqdar.php) | 2114 | **البوابات الثلاث + مسارات الكتابة + الصفحات العامة** |
| [Taqdar_gate.php](application/controllers/Taqdar_gate.php) | 467 | **بوابة الإتقان JSON — 16 نقطة** |
| [Taqdar_admin.php](application/controllers/Taqdar_admin.php) | 634 | وحدات الإدارة |
| [Taqdar_cron.php](application/controllers/Taqdar_cron.php) | — | `expire` · `expire_sessions` |
| [Taqdar_cron_events.php](application/controllers/Taqdar_cron_events.php) | — | الأحداث الستة + الأسبوعي |

### النماذج

| الملف | الأسطر | ما فيه |
|---|---|---|
| [Taqdar_repo_model.php](application/models/Taqdar_repo_model.php) | 1526 | **بوابة الإتقان · المراجعة · دفتر الأخطاء · التوأم · كتالوج الأخطاء** |
| [Taqdar_wallet_model.php](application/models/Taqdar_wallet_model.php) | 1153 | **المحفظة بالقيود · السحب · التسوية** |
| [Taqdar_parent_model.php](application/models/Taqdar_parent_model.php) | 1211 | **دورة الربط والموافقة · المؤشرات · الرسائل** |
| [Taqdar_teacher_model.php](application/models/Taqdar_teacher_model.php) | 1003 | الرفع · الكورسات · استيراد الأسئلة |
| [Taqdar_billing_model.php](application/models/Taqdar_billing_model.php) | 804 | الباقات · الاشتراكات · الفواتير · الاستحقاق |
| [Taqdar_marking_model.php](application/models/Taqdar_marking_model.php) | 795 | **التصحيح باعتماد المعلم** |
| [Taqdar_sessions_model.php](application/models/Taqdar_sessions_model.php) | 621 | المواعيد · الحجز · القرار |
| [Taqdar_events_model.php](application/models/Taqdar_events_model.php) | 475 | **الإشعارات الخمسة + منع التكرار بالبصمة** |
| [Taqdar_site_model.php](application/models/Taqdar_site_model.php) | 551 | بيانات الموقع العام |
| [Taqdar_admin_model.php](application/models/Taqdar_admin_model.php) | 708 | وصف وحدات CRUD |

### المساعدات (تحمل تلقائيا — `autoload.php`)

| الملف | ما فيه |
|---|---|
| [taqdar_role_helper.php](application/helpers/taqdar_role_helper.php) | **`tq_role()` · `tq_guard()` · `tq_home_for()`** |
| [taqdar_admin_helper.php](application/helpers/taqdar_admin_helper.php) | **`tqa_ltr()` · `tqa_money()` · `tqa_cell()`** |
| [taqdar_helper.php](application/helpers/taqdar_helper.php) | أدوات عامة |
| [taqdar_icons_helper.php](application/helpers/taqdar_icons_helper.php) | أيقونات SVG |
| [taqdar_site_helper.php](application/helpers/taqdar_site_helper.php) | `tq_site_asset()` · `tqs_bank_block()` |

### القوالب والأصول

```
application/views/frontend/taqdar/       78 قالبا
  index.php · header.php · footer.php      غلاف الثيم
  portal_rail.php · portal_topbar.php      قائمة البوابة وترويستها
  portal_open.php · portal_close.php       فتح وإغلاق غلاف البوابة
  home.php                                 الصفحة الرئيسية
  site/                                    10 قطع مشتركة للموقع العام
  tq_*.php                                 34 شاشة بوابة
  site_*.php                               9 صفحات موقع

assets/taqdar/
  css/tokens.css        ← نظام التصميم (المصدر الوحيد للألوان والخطوط)
  css/base.css · components.css · layout.css · fonts.css · admin.css
  js/taqdar.js · taqdar-lesson.js · taqdar-reviews.js
  brand/ · fonts/ · site/ (css · js · img · svg · video · files)
```

### التوجيه والنشر

| الملف | ما فيه |
|---|---|
| [routes.php](application/config/routes.php) | **232 سطرا — الترتيب مقصود: الكتابة قبل العرض** |
| [.htaccess](.htaccess) | 301 من `taqdar/*` إلى `/student/*` · `/courses`→`/plans` · حجب `.git`/`server`/`scripts` |
| [deploy.sh](deploy.sh) | `git reset --hard origin/main` + نسخ احتياطي + تنظيف كاش + **فحص 200 وإلا يفشل** |
| [server/](server/) | مقلع الكرون + crontab — نسخ مرجعية لملفات تعيش **فوق** جذر الويب |
| [scripts/strip_tashkeel_db.php](scripts/) | تجريد التشكيل من القاعدة — `--apply` للتنفيذ |

---

## ملحق: القواعد التي لا تكسر — بطاقة مرجعية

قواعد مستخرجة من الوثيقتين ومن الشيفرة، مجموعة في مكان واحد:

### من المخطط

1. **كل منطق عمل في الخادم.** العميل يعرض ويجمع مدخلات فقط. القفل والخصم
   والدرجة تحسب في الخادم — وأربعة عملاء يعني أربع نسخ من المنطق ستتباعد حتما.
2. **الدرس التالي مقفل على الخادم.** طلب مباشر لدرس غير مفتوح يرجع 403 بلا
   رابط تشغيل. القفل البصري تجميل.
3. **النطاق يفرض في طبقة الاستعلام لا في الواجهة.** إخفاء زر ليس صلاحية.
4. **الأموال أعداد صحيحة بأصغر وحدة.** لا حساب بأرقام عشرية عائمة.
5. **قيود المحفظة تصحح بقيد عكسي لا بمسح.**
6. **سجل التدقيق ملحق فقط.** الجدول يحفظ القيم كما كانت.
7. **لا ترتيب ولا مقارنة بين الطلاب.** لا لوحة صدارة عامة.
8. **الدرجة النهائية للمقالي لا تظهر قبل اعتماد المعلم صراحة.**
9. **الاتجاه بـ`start/end` حصرا.** أي `left/right` في التخطيط عيب.
10. **كل رقم لاتيني في نص عربي يمر بمكون عزل واحد.** لا استثناءات.
11. **الحدث يصف ما حدث** (`object_verb_past`) لا ما ستفعله الواجهة به.
12. **لا تظهر رسالة خطأ لا يستطيع المستخدم فعل شيء حيالها.**

### من هذا المستودع

13. **مسارات الكتابة تعلن قبل مسارات العرض.** `(:any)` في CI3 مقطع واحد، وبلا
    قاعدة صريحة يسقط `teacher/upload/save` إلى `Taqdar::teacher('upload','save')`
    — فتعرض الشاشة ردا على طلب حفظ: لا حفظ، ولا خطأ، ولا رسالة.
14. **مسارات الحفظ POST فقط**، وترفض GET بـ`show_404()`.
15. **`system/` و`application/libraries/` طرف ثالث** — لا تعدل.
16. **ملفات `.orig` و`.pre-*` و`.bak-*` جامدة** — لا تحرر ولا تقرأ كمصدر للحقيقة.
17. **الأسرار لا ترفع.** المستودع عام؛ `database.php` و`taqdar_secret.php`
    مستثنيان، ولكل قالب `.example`. أو تخزن في `settings`.
18. **الواجهة عربية RTL بلا تشكيل.** U+064B–U+0652 ممنوعة في أي نص.
19. **الميزات الجديدة تكتب في طبقة `Taqdar_*`** لا في `Crud_model.php` أو `Admin.php`.
20. **الأدمن لا يستثنى من حراس الأدوار.** خلط الأدوار يجعل الاختبار كاذبا.
21. **لا تحميل نموذج داخل عرض بـ`$this->load`** — يستعمل `get_instance()`،
    لأن الأول ينتج بترا صامتا للصفحة لا خطأ ينبه.
22. **نماذج الكرون لا تقرأ `$this->session`** — لا جلسة في سطر الأوامر،
    وقراءتها تسقط المهمة بعد أن تكون قد غيرت البيانات.

---

*آخر تحديث: 2026-08-11 · مبني على «مخطط النظام 1.0» و«فلو المستخدمين وخطة
السبرنتات 1.0» مقابل الشجرة عند `072455a` على `main`.*
