<?php
/**
 * مُشغّل مهامّ تقدّر من سطر الأوامر.
 *
 * `config.php` يبني `base_url` من `$_SERVER['HTTP_HOST']`، وهو غير موجود في
 * سطر الأوامر. فينبعث تحذير **أثناء تحميل الإعدادات نفسها**، فيستدعي
 * `log_message()` صنفَ السجلّ ومصفوفة الإعدادات نصف ممتلئة — ومن هنا جاءت
 * تحذيرات `log_path` المتتالية. ضبط المضيف قبل الإقلاع يقطع السلسلة من أصلها،
 * ويجعل الروابط المولَّدة في المهامّ الدورية صحيحة أيضًا.
 *
 * وهو خارج جذر الويب عمدًا: ملفّ إقلاع يمهّد البيئة لا يُترك في متناول الطلب.
 */
$_SERVER['HTTP_HOST']   = 'taqdaredu.com';
$_SERVER['HTTPS']       = 'on';
$_SERVER['SERVER_NAME'] = 'taqdaredu.com';

chdir(__DIR__ . '/public_html');
require __DIR__ . '/public_html/index.php';
