# تقرير مراجعة وتدقيق: الدفع وبوابات الدفع والكورسات

**المشروع:** My-Communication Academy (`my-communication.uk`) — Academy LMS
**التاريخ:** 2026-07-13
**النطاق:** منظومة الدفع (`Api_payment`)، بوابات الدفع (TAP / PayPal / القديمة)، الويب هوكس (`Api_webhooks` + `Payment::tap_webhook`)، الكورسات والتحكم في الوصول (`Api_courses`)، التقارير المالية (`Api_reports`)، والمصادقة (`TokenHandler` / `JWT`).
**نوع التقرير:** مراجعة أمنية ووظيفية (Audit) — نقاط ضعف + مشاكل + فرص تحسين.

---

## 1. الملخص التنفيذي

المنظومة **مبنية بشكل جيد في مسار الدفع الفعّال (Active checkout)**: الأسعار تُحسب في الخادم لا من العميل، والتحقق من الدفع يتم فعلياً مع بوابة الدفع (TAP + PayPal) مع فحص المبلغ والعملة وحماية من إعادة التشغيل (replay). المصادقة (JWT) قوية. التحكم في الوصول للفيديوهات المدفوعة محكم.

لكن توجد **مشكلتان بخطورة عالية** تحتاجان إصلاحاً، ومجموعة من المشاكل المتوسطة/المنخفضة أغلبها في **كود قديم/مكرر** (dead & duplicated code) ما زال قابلاً للاستدعاء ويحمل ضمانات أضعف من المسار الحديث.

| # | الخطورة | العنوان | الملف الرئيسي |
|---|---------|---------|----------------|
| P1 | 🔴 عالية | ويب هوك TAP: التحقق من التوقيع اختياري (fail-open) + مسار التفعيل الاحتياطي معطّل → دفع بلا تسجيل | `Tap_payment_model.php` |
| P2 | 🔴 عالية | SSRF عبر الويب هوكس المُعرّفة من المستخدم (test/retry) بلا حظر للعناوين الداخلية | `Api_webhooks.php` |
| P3 | 🟠 متوسطة | مسار `initiate`/`verify` القديم قابل للاستدعاء: إعادة استخدام عملية PayPal + خطأ كشف sandbox | `Api_payment.php` |
| P4 | 🟠 متوسطة | تكرار منطق المصادقة/CORS في كل كنترولر + تعارض قائمة الأصول في `Api_payment` | كل `Api_*` |
| P5 | 🟡 منخفضة | تسريب روابط الفيديو للطلاب منتهي اشتراكهم عبر `detail`/`curriculum` (لا يُفحص الانتهاء) | `Api_courses.php` |
| P6 | 🟡 منخفضة | تسجيل بيانات شخصية (PII) كاملة في اللوجات | `Tap_payment_model.php` |
| P7 | 🟡 منخفضة | حساب تقدّم الطالب يقرأ صفاً واحداً من `watch_histories` → عدّ خاطئ محتمل | `Api_reports.php` |
| P8 | 🟡 منخفضة | منطق الكوبونات بلا حد استخدام كلي/حد أدنى؛ التقاط PayPal لا يفحص العملة | `Api_payment.php` |
| P9 | ⚪ ملاحظة | حساب الأدمن يُعرّف بإيميل ثابت؛ `RAND()`؛ قيمة `stripe` قديمة في الفرونت | متعدد |

---

## 2. نقاط القوة (تعمل بشكل صحيح)

قبل المشاكل، من المهم توثيق ما هو محكم فعلاً حتى لا يُكسر أثناء الإصلاح:

- **الأسعار تُحسب في الخادم:** `checkout_post()` يحسب الإجمالي من قاعدة البيانات عبر `get_cart_total()` ولا يثق بأي مبلغ من العميل → لا يمكن التلاعب بالسعر. `Api_payment.php:944`
- **تحقق فعلي من الدفع في المسار الحديث:**
  - TAP: `tap_success_get()` يسترجع الشحنة من TAP API ويتأكد أن الحالة `CAPTURED/AUTHORIZED`، ويطابق **المبلغ + العملة**، ويمنع إعادة الاستخدام (ربط `tap_id` بالجلسة). `Api_frontend.php:1057`
  - PayPal: `payment_success_get()` + `verify_payment_with_gateway()` يتأكدان من حالة `COMPLETED` والمبلغ ≥ المتوقع قبل التسجيل، مع حارس إعادة تشغيل. `Api_frontend.php:909`
- **مصادقة JWT سليمة:** `JWT::decode` يفرض قائمة خوارزميات مسموحة (`HS256` فقط) → لا هجوم `alg:none` أو خلط مفاتيح؛ و`TokenHandler` يفرض سرّاً ≥ 32 حرفاً ويضبط `exp` (30 يوماً). `TokenHandler.php:36`, `JWT.php:105`
- **تحكم وصول محكم للفيديو:** `_can_watch_respond()` يتحقق من التسجيل والانتهاء والدرس المجاني والمالك والأدمن، ولا يُرجع `video_url` إلا عند السماح. `Api_courses.php:431`
- **رفع ملف الدفع الأوفلاين محصّن:** allowlist للامتداد + فحص MIME عبر `finfo` + اسم عشوائي + حد حجم. `Api_payment.php:453`
- **إغلاق ثغرة إعادة الكوبون:** كوبون 100% يُسجَّل صف `payment` حتى لا يُعاد استخدامه بلا نهاية. `Api_payment.php:702`
- **CORS مقيّد** بقائمة أصول محددة (لا wildcard).

---

## 3. النتائج التفصيلية

### 🔴 P1 — ويب هوك TAP: توقيع اختياري + مسار تفعيل احتياطي معطّل

**الملفات:** `api/application/models/Tap_payment_model.php:302` (والكنترولر `Payment.php:897`)

**أ) التحقق من التوقيع يفشل مفتوحاً (fail-open):**
```php
$hashstring = isset($_SERVER['HTTP_HASHSTRING']) ? $_SERVER['HTTP_HASHSTRING'] : '';
...
if ($hashstring && !$this->validate_webhook_hashstring($webhook_data, $hashstring, $api_key)) {
    return false;   // يُرفض فقط لو أُرسل توقيع خاطئ
}
```
لو لم يرسل المهاجم ترويسة `HASHSTRING` إطلاقاً، فإن `$hashstring` فارغة ويُتخطّى التحقق بالكامل. أي أن `payment/tap_webhook` يقبل جسم POST مزوّراً بلا أي توقيع، ويثق بحقل `status` القادم في الجسم دون الرجوع إلى TAP API ودون فحص المبلغ.

**ب) مسار التفعيل الاحتياطي معطّل وظيفياً (والأخطر عملياً):**
الويب هوك يُفعّل من `users.cart_items` (المخزن القديم الميت — الشيك أوت الحديث يستخدم جدول `cart` ولقطة `payment.course_ids`):
```php
$cart_ids = json_decode($user['cart_items'] ?: '[]', true) ?: [];
if (empty($cart_ids)) { return true; }   // لا تسجيل
```
**الأثر:** لأن `cart_items` فارغ دائماً في تدفّق الـSPA، فإن الويب هوك **لا يُفعّل أي كورس فعلياً**. لذا لو دفع المستخدم على صفحة TAP ثم لم يرجع إلى `tap_success` (أغلق التبويب/انقطعت الشبكة)، فهو **دفع ولم يُسجَّل** ولا يوجد مسار احتياطي ينقذه. هذا خطر مالي حقيقي (أموال بلا خدمة). كما أن الويب هوك لا يحدّث `payment.status` إلى `completed`.

**التوصية:**
1. اجعل التوقيع **إلزامياً**: ارفض أي طلب بلا `HASHSTRING` صالح.
2. بعد التحقق من التوقيع، استرجع الشحنة من TAP API وطابق المبلغ/العملة (نفس منطق `tap_success_get`).
3. فعّل من **نفس لقطة `payment.course_ids`** المستخدمة في `finalize_purchase()`، لا من `users.cart_items`، وحدّث `payment.status = completed` بشكل idempotent — بحيث يصبح الويب هوك مسار تفعيل احتياطياً حقيقياً.

> **ملاحظة على الخطورة:** الاستغلال الأمني المباشر (تسجيل مجاني عبر تزوير الويب هوك) *محدود حالياً* بسبب اعتماد المخزن الميت `cart_items`، لكن التصميم fail-open + المسار المعطّل يجعلان هذا بنداً عالي الأولوية (مالياً وأمنياً).

---

### 🔴 P2 — SSRF عبر الويب هوكس المُعرّفة من المستخدم

**الملف:** `api/application/controllers/Api_webhooks.php:113` (`create_post`) و`:429` (`send_webhook`) عبر `test_post:255` / `retry_post:392`

أي مستخدم مُصادَق (والتسجيل الذاتي متاح) يستطيع تسجيل عنوان ويب هوك عشوائي ثم استدعاء `test`/`retry` ليجعل **الخادم** يرسل POST إلى ذلك العنوان. التحقق الحالي يقتصر على:
```php
if (!filter_var($url, FILTER_VALIDATE_URL)) ...        // شكل URL فقط
if (strpos($url, 'https://') !== 0 && ENVIRONMENT !== 'development') ... // HTTPS فقط
```
لا يوجد **حظر للعناوين الداخلية/الخاصة**: `127.0.0.1`, `::1`, `169.254.169.254` (Cloud metadata), نطاقات `10.x/172.16.x/192.168.x`, ولا حظر منافذ غير قياسية. والأسوأ أن جسم الرد يُخزَّن ويُعاد للمستخدم في رد `test_post` (SSRF مع انعكاس/تسريب للرد).

**الأثر:** استطلاع الشبكة الداخلية والوصول لخدمات/لوحات إدارة داخلية عبر HTTPS، مع احتمال قراءة الرد. مُقيّد جزئياً باشتراط HTTPS في الإنتاج وبعدم اتباع التحويلات (`CURLOPT_FOLLOWLOCATION` غير مفعّل).

**التوصية:** قبل أي طلب صادر، حلّ اسم النطاق (DNS) وارفض إن كان يشير إلى نطاق خاص/loopback/link-local؛ فعّل allowlist للمنافذ (443)؛ لا تُعِد جسم الرد الكامل للمستخدم؛ واعتبر أن الميزة إن لم تكن مستخدمة فعلاً يُفضّل تعطيلها.

---

### 🟠 P3 — مسار `initiate`/`verify` القديم ما زال قابلاً للاستدعاء

**الملف:** `Api_payment.php` — `initiate_post:134`, `verify_post:230`, `get_payment_info:1055`, `verify_with_gateway:1083`

الـSPA يستخدم **`checkout` فقط** (تأكدنا من `services.ts:353`)، لكن مسار `initiate`/`verify` القديم ما زال مُوجّهاً عبر `api/v2/payment/initiate|verify` ويحمل ضمانات أضعف:

- **إعادة استخدام عملية PayPal:** `verify_post` لا يربط `transaction_id` بشكل فريد (بخلاف `tap_success` الذي يمنع إعادة الاستخدام). عمليّة PayPal مكتملة بنفس المبلغ يمكن تمريرها للتحقق أكثر من مرة → التسجيل في أكثر من كورس بنفس السعر بدفعة واحدة.
- **خطأ منطقي في كشف sandbox:** `get_settings('paypal') == 'sandbox'` — بينما `paypal` مخزّن كـ JSON (يُفكّ عبر `json_decode` في باقي الكود). المقارنة دائماً `false` → يستخدم دائماً مفاتيح live ومفاتيح إعدادات لا تطابق بنية الـJSON.
- **تعارض سطح الواجهة:** `methods_get()` ما زال يعلن razorpay/paytm/flutterwave/paystack بينما `checkout_post` يرفض كل شيء عدا `tap`/`paypal`.

**التوصية:** احذف مسار `initiate`/`verify`/`get_payment_info`/`verify_with_gateway` بالكامل (كود ميت بضمانات أضعف = فخ مستقبلي)، ونظّف `methods_get()` ليطابق البوابات المفعّلة فعلاً.

---

### 🟠 P4 — تكرار منطق المصادقة/CORS + تعارض قائمة الأصول

**الملفات:** كل `Api_payment` / `Api_courses` / `Api_reports` / `Api_webhooks`

كل كنترولر يعيد تعريف `authenticate()` وقائمة أصول CORS يدوياً. النتيجة: أي إصلاح لازم يُكرَّر في عدة أماكن، وقد ظهر بالفعل تعارض — قائمة `Api_payment.php:28` تفتقد `https://www.my-communication.uk` الموجودة في باقي الكنترولرات. (أثره محدود لأن `.htaccess` يوحّد إلى non-www، لكنه دليل على مخاطر التكرار).

**التوصية:** انقل المصادقة و CORS إلى `REST_Controller` (أو Trait/Base مشترك) بمصدر واحد للحقيقة.

---

### 🟡 P5 — تسريب روابط الفيديو للطلاب منتهي اشتراكهم

**الملف:** `Api_courses.php:236` (`detail_get`) و`:341` (`curriculum_get`)

يُحسب `is_enrolled` عبر وجود صف في `enrol` (`num_rows() > 0`) **دون فحص `expiry_date`**، بينما `can_watch` يفحص الانتهاء. فطالب انتهت مدته يستطيع سحب `video_url` من `detail`/`curriculum` رغم أن `can_watch` سيمنع التشغيل.

**التوصية:** طبّق نفس فحص الانتهاء المستخدم في `_can_watch_respond` عند حساب `full_access`/`is_enrolled` في هذين المسارين.

---

### 🟡 P6 — تسجيل بيانات شخصية (PII) في اللوجات

**الملف:** `Tap_payment_model.php:304`

```php
log_message('info', 'Tap Webhook Received: ' . json_encode($webhook_data));
```
يسجّل الحمولة كاملة (إيميل/هاتف/بيانات عملية العميل). **التوصية:** سجّل الحقول الضرورية فقط (معرّف الشحنة/الحالة/المبلغ) أو نقّح الحقول الحساسة.

---

### 🟡 P7 — حساب تقدّم الطالب قد يكون خاطئاً

**الملف:** `Api_reports.php:491` (`get_completed_lessons_count`)

يقرأ **صفاً واحداً فقط** من `watch_histories` (`->row_array()`) ويستخدم `completed_lessons` منه، بينما `student_activity_get` يتعامل مع `watch_histories` كصفوف لكل درس. هذا تعارض في نموذج البيانات قد يؤدي لعدّ دروس مكتملة خاطئ (وعدم تطابق مع `mycom_completed_lesson_ids` المستخدم في الكنترولر). **التوصية:** وحّد مصدر «الدروس المكتملة» عبر `mycom_completed_lesson_ids`.

---

### 🟡 P8 — منطق الكوبونات والعملة (Defense-in-depth)

- **الكوبونات:** `apply_coupon` (`Api_payment.php:975`) بلا حد استخدام كلي للكوبون، ولا حد أدنى للشراء، ويطبَّق على المتجر كله (الحد الوحيد هو مرة/مستخدم). سلوك عمل مقبول لكن يستحق مراجعة تجارية.
- **PayPal:** التقاط PayPal في `verify_payment_with_gateway` يطابق المبلغ لكن **لا يطابق العملة** (بخلاف TAP). الخطر منخفض (الطلب يُنشأ بعملة ثابتة في الخادم) لكن يُنصح بإضافة فحص العملة للتوحيد.

---

### ⚪ P9 — ملاحظات إضافية

- **حساب الأدمن super-viewer** يُعرّف بإيميل ثابت `admin@my-communication.uk` (`Api_courses.php:47`) بدل علم دور (role flag) — هشّ ويعتمد على تفرّد الإيميل.
- **`order_by('RAND()')`** في `get_related_courses` (`:1195`) — أداء ضعيف على جداول كبيرة.
- **قيمة `stripe` قديمة في الفرونت:** `services.ts:627` و`useApi.ts:851` يمرّران `'stripe'` بينما الباك يرفضها؛ تنظيف بسيط لعقد الواجهة.

---

## 4. أولويات الإصلاح المقترحة

**الآن (خطورة عالية / أثر مالي):**
1. **P1** — اجعل توقيع ويب هوك TAP إلزامياً + أعِد بناء التفعيل الاحتياطي من لقطة `payment.course_ids` (يمنع «دفع بلا تسجيل»).
2. **P2** — أضف حظر العناوين الداخلية (SSRF) للويب هوكس أو عطّل الميزة إن لم تُستخدم.

**قريباً (تنظيف يقلّل سطح الخطر):**
3. **P3** — احذف مسار `initiate`/`verify` القديم ونظّف `methods_get()`.
4. **P4** — وحّد المصادقة/CORS في أساس مشترك.
5. **P5** — أضف فحص انتهاء الاشتراك في `detail`/`curriculum`.

**تحسينات:**
6. **P6/P7/P8/P9** — تنقية اللوجات، توحيد حساب التقدّم، فحص عملة PayPal، واستبدال إيميل الأدمن بعلم دور.

---

*ملاحظة: هذا التقرير يعتمد على قراءة الكود (static review) في `c:\xampp\htdocs\body\api`. لم يتم اختبار الاستغلال حياً على الخادم. يُفضّل التحقق من كل بند في بيئة staging قبل النشر.*
