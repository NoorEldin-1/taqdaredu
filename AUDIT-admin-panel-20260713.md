# تقرير مراجعة وتدقيق: لوحة تحكم الأدمن (Admin Panel)

**المشروع:** My-Communication Academy (`my-communication.uk`) — Academy LMS
**النطاق:** لوحة تحكم الأدمن القديمة المبنية بـ CodeIgniter 3 (`/api/admin/*`) — الكنترولر `Admin.php` (3673 سطر)، آلية تسجيل الدخول (`Login.php`)، الجلسات والصلاحيات (`User_model`, `common_helper`)، والـ views تحت `views/backend/admin/` (152 ملف).
**السؤال الأساسي:** هل اللوحة متربطة مظبوط بالمشروع؟ + ما المشاكل فيها؟
**نوع التقرير:** مراجعة أمنية ووظيفية وتكامل (Audit) — **مافيش أي تعديل على الكود، ده تقرير فقط.**
**التاريخ:** 2026-07-13

---

## 1. الملخص التنفيذي

**هل اللوحة متربطة بالمشروع؟ — أيوه، مربوطة صح في الجوهر.** اللوحة بتقرأ وتكتب على **نفس قاعدة بيانات** الـ React SPA والـ JWT API بالظبط (`course`, `category`, `users`, `enrol`, `payment`, `coupons`, `payment_gateways`, `lesson`, `section`…). يعني أي كورس/كوبون/كاتيجوري/مستخدم بتعمله من اللوحة **بيظهر فعليًا** على الموقع الحي. وتحديدًا: الكوبونات اللي بتضيفها من اللوحة بتتخزّن في جدول `coupons` بنفس الأعمدة (`code`, `discount_percentage`, `expiry_date`) اللي بيقراها منطق الخصم في `Api_payment` — يعني متوافقين 100%.

**لكن فيه نقطة انفصال جزئي واحدة مهمة:** بوّابات الدفع. اللوحة تقدر تفعّل بوابات (Stripe/Razorpay/Paytm/Flutterwave…) لكن الـ checkout في الـ SPA بيدعم **TAP + PayPal بس** — فتفعيل أي بوابة تانية من اللوحة **ملهوش أي أثر** على الدفع الفعلي في الموقع.

**أخطر المشاكل** مالهاش علاقة بالربط، لكنها **أمنية** في اللوحة نفسها: تسجيل دخول الأدمن **مفتوح للتخمين (brute-force)** بلا أي حماية، و**حماية الـ CSRF متعطّلة بالكامل** مع إجراءات حذف بتتنفّذ عبر روابط GET.

| # | الخطورة | العنوان | المكان |
|---|---------|---------|--------|
| A1 | 🔴 عالية | تسجيل دخول الأدمن بلا أي حماية ضد التخمين (لا rate-limit، لا CAPTCHA، لا 2FA) — و5 حسابات أدمن | `Login.php:48` |
| A2 | 🔴 عالية | حماية CSRF متعطّلة عالميًا + إجراءات حذف عبر GET | `config.php:465` + `Admin.php` |
| A3 | 🟠 متوسطة | كوكي الجلسة قد يُرسَل بلا `Secure` خلف بروكسي/Cloudflare + لا `SameSite` صريح | `config.php:412` |
| A4 | 🟠 متوسطة | دوال كتير بلا `check_permission()` → أدمن مقيّد يتخطّى صلاحياته (وتصدير CSV لبيانات حساسة) | `Admin.php` (متعدد) |
| A5 | 🟡 منخفضة | انفصال بوابات الدفع: اللوحة تفعّل بوابات لا يدعمها checkout الـ SPA | `Admin.php:737` |
| A6 | 🟡 منخفضة | تمرير JWT في الـ URL عند دخول Google (يُسجَّل في اللوجات) | `Login.php:248` |
| A7 | 🟡 منخفضة | جلسة 10 أيام بلا ربط IP/User-Agent | `config.php:385` |
| A8 | ⚪ ملاحظة | نموذج الصلاحيات: أدمن بلا صفوف صلاحيات = صلاحية كاملة | `common_helper.php:103` |

---

## 2. نقاط القوة (شغّالة صح ومربوطة)

- **طبقة بيانات موحّدة:** اللوحة والـ SPA والـ API بيشتغلوا على نفس جداول MySQL — مفيش «قاعدة بيانات تانية» أو مزامنة منفصلة. أي تعديل في اللوحة فوري على الموقع.
- **الكوبونات مربوطة تمام:** `Crud_model::add_coupon()` (`models/Crud_model.php:4353`) بيعمل `insert('coupons', ...)` بأعمدة `code`/`discount_percentage`/`expiry_date` — نفس اللي بيقراه `Api_payment::apply_coupon()`. الكوبون اللي بتعمله من اللوحة بيشتغل في سلة الطالب مباشرةً.
- **مصادقة الكونترولر مركزية:** الـ constructor في `Admin.php:18` بينادي `check_session_data('admin')` اللي بيرفض أي غير-أدمن ويحوّله للّوجين — فمفيش أي دالة في اللوحة متاحة للعامة بدون جلسة أدمن صالحة (حتى الدوال اللي «ناقصة» فحص `admin_login` جواها محمية بالـ constructor).
- **تسجيل الدخول موحّد (SSO):** نفس صفحة اللوجين بتخدم الأدمن (→ جلسة كوكي → `admin/dashboard`) والطالب (→ JWT → React SPA). ودخول Google بيبني JWT ويحوّل للـ React.
- **DataTables آمنة من SQLi:** ترتيب الأعمدة متفلتر بقائمة بيضاء (`$columns`) والبحث بيستخدم `like()` المُهرَّب — مفيش حقن SQL في الجداول الخادمية (`server_side_users_data` وأخواتها).
- **كلمات المرور:** اللوجين بيدعم bcrypt مع ترقية تلقائية من sha1 القديم (`Login.php:64-69`) — كويس.
- **httponly مفعّل** على الكوكيز (`config.php:413`).

---

## 3. النتائج التفصيلية

### 🔴 A1 — تسجيل دخول الأدمن مفتوح للتخمين (brute-force)

**الملف:** `controllers/Login.php:48` (`validate_login`) + حالة الإعدادات الحية.

دالة `validate_login()` بتقارن الإيميل/الباسورد وبتحوّل على طول من غير:
- **أي rate-limiting / lockout** بعد محاولات فاشلة (على عكس الـ JWT API login اللي اتعمله حماية).
- **CAPTCHA فعّالة:** الكود بيطلب reCAPTCHA فقط لو مفعّلة في الإعدادات، وفي قاعدة البيانات الحية `recaptcha_status = 0` و`recaptcha_status_v3 = 0` والمفاتيح مجرد placeholder (`Valid-secret-key`). يعني **مفيش CAPTCHA فعليًا**.
- **مفيش 2FA** على دخول الويب للأدمن.

**الأثر:** فيه **5 حسابات أدمن** (`role_id=1`). أي مهاجم يقدر يجرّب كلمات سر بلا حدود على `/api/login`. ده أخطر بند لأنه مؤكَّد **غير محمي على السيرفر الحي دلوقتي**.

**التوصية:** أضِف rate-limiting/تأخير تصاعدي + قفل مؤقت بعد N محاولات فاشلة (نفس أسلوب حماية الـ API login)، فعّل reCAPTCHA بمفاتيح حقيقية، ويُفضّل 2FA لحسابات الأدمن.

---

### 🔴 A2 — حماية CSRF متعطّلة + إجراءات حذف عبر GET

**الملفات:** `config/config.php:465` (`$config['csrf_protection'] = FALSE;`) + `Admin.php` (روابط الحذف).

الـ CSRF متعطّل عالميًا، واللوحة بتستخدم مصادقة كوكي جلسة، وكتير من الإجراءات المدمّرة بتتنفّذ عبر **روابط GET**:
```php
// Admin.php — رابط حذف مستخدم (server_side_users_data:296)
'... onclick="confirm_modal(\'' . site_url('admin/users/delete/'.$student['id']) . '\')"'
// وكذلك admin/coupons/delete/ID (2023) و admin/categories/delete/ID (71) وغيرها
```
مفيش `cookie_samesite` مضبوط صراحةً في الإعدادات. المتصفحات الحديثة بتفترض `Lax` افتراضيًا (بيحمي جزئيًا من POST عبر المواقع لكن بيسمح بالتنقّل العلوي عبر GET) — لكن الاعتماد على افتراض المتصفح مش بديل عن توكن CSRF.

**الأثر:** صفحة خبيثة يزورها أدمن مسجّل ممكن تدفعه (عبر تنقّل/رابط) لتنفيذ حذف مستخدم/كورس/كوبون/تصنيف أو تغيير إعدادات بدون علمه.

**التوصية:** فعّل `csrf_protection = TRUE` (مع `csrf_exclude_uris` للـ webhooks وأي endpoints machine-to-machine)، وحوّل كل الإجراءات المدمّرة من GET لـ POST مع توكن، واضبط `cookie_samesite = 'Lax'` (أو `Strict`) صراحةً.

> ملاحظة: تفعيل CSRF محتاج تحديث الـ forms والـ AJAX في الـ views عشان تبعت التوكن — تغيير واسع، يُنفَّذ ويُختبَر على staging.

---

### 🟠 A3 — كوكي الجلسة قد يُرسَل بلا `Secure`

**الملف:** `config/config.php:412`

```php
$config['cookie_secure'] = ((isset($_SERVER['HTTPS']) && $_SERVER['HTTPS'] == "on") ? TRUE : FALSE);
```
خلف بروكسي/Cloudflare بينهي الـ TLS ويمرّر HTTP للأصل، ممكن `$_SERVER['HTTPS']` ما تكونش `"on"` → الكوكي يتضبط **بدون Secure** → قابل للتسريب على HTTP. كمان مفيش `SameSite` صريح.

**التوصية:** اضبط `cookie_secure = TRUE` بشكل ثابت (الموقع HTTPS-only أصلًا عبر `.htaccess`)، أو اعتمد على `X-Forwarded-Proto` من البروكسي. وأضِف `cookie_samesite`.

---

### 🟠 A4 — دوال بلا `check_permission()` (تخطّي صلاحيات بين الأدمنية)

**الملف:** `Admin.php` — دوال بتفتقد `check_permission()`:
`get_courses`, `notification_settings`, `sitemap_settings`, `review`, `review_edit`, `badges_add`, `badges_edit`, `export_enrol_history_csv`, `export_student_progress_csv`, `export_admins_csv`, ومجموعة `ajax_*`.

كل الدوال دي محمية بجلسة أدمن (من الـ constructor)، **لكن** مش محمية بفحص الصلاحية التفصيلي. يعني **أدمن مقيّد** (عنده صفوف صلاحيات محدّدة) يقدر يوصل لوظائف خارج صلاحياته — أخطرها **تصدير CSV** لبيانات الأدمنية (`export_admins_csv`) وتقدّم الطلاب (`export_student_progress_csv`)، وتغيير إعدادات عامة (`notification_settings`, `sitemap_settings`).

في قاعدة البيانات الحية: **5 حسابات أدمن** و**5 صفوف في جدول `permissions`** — يعني نظام الصلاحيات مُستخدَم جزئيًا فعلًا، فالبند ده واقعي مش نظري بحت.

**التوصية:** أضِف `check_permission('<القسم المناسب>')` لأول كل دالة من دي (زي باقي الدوال).

---

### 🟡 A5 — انفصال بوابات الدفع بين اللوحة والـ SPA

**الملف:** `Admin.php:737` (`payment_settings`) + `Api_payment::methods_get/checkout_post`.

اللوحة بتدير جدول `payment_gateways` (فيه tap, razorpay, xendit, payu, flutterwave… إلخ) + إعدادات JSON قديمة (paypal/stripe/razorpay/paytm). لكن الـ SPA بيعرض ويقبل **TAP + PayPal بس**. حاليًا المفعّل فعليًا هو `paypal` و`tap` فقط (الباقي status=0)، فالمشكلة **كامنة**: لو أدمن فعّل بوابة تانية من اللوحة، هتظهر «مفعّلة» لكن **مش هتشتغل** في checkout الـ SPA — تجربة مربكة.

**ملاحظة تصميمية:** PayPal متخزّن في مكانين (صف في `payment_gateways` + مفتاح JSON في `settings.paypal` اللي الـ SPA بيقراه) — مصدرين للحقيقة، مربك للصيانة.

**التوصية:** في واجهة `payment_settings` أخفِ/علّم البوابات غير المدعومة في الـ SPA (سِيبها TAP + PayPal بس)، ووحّد مصدر إعداد PayPal.

---

### 🟡 A6 — تمرير JWT في الـ URL عند دخول Google

**الملف:** `Login.php:248`

```php
$react_url = 'https://my-communication.uk/login?google_auth=' . base64_encode(json_encode($userdata)); // فيه token
redirect($react_url, 'refresh');
```
التوكن بيتحطّ في query string — بيتسجّل في لوجات السيرفر/البروكسي وتاريخ المتصفح والـ Referer. **التوصية:** استخدم fragment (`#`) بدل query، أو كود تبادل قصير العمر (one-time code) بدل تمرير الـ JWT مباشرةً.

---

### 🟡 A7 — جلسة طويلة بلا ربط IP/User-Agent

**الملف:** `config/config.php:385-390` — `sess_expiration = 864000` (10 أيام)، `sess_match_ip = FALSE`، `sess_match_useragent = FALSE`. كوكي جلسة أدمن مسروق يفضل صالح 10 أيام من أي مكان. **التوصية:** قلّل مدة جلسة الأدمن، وفكّر في `sess_match_ip`/تدوير التوكن — مع موازنة الراحة.

---

### ⚪ A8 — ملاحظة على نموذج الصلاحيات

`has_permission()` (`common_helper.php:103-127`) بترجّع **صلاحية كاملة** لو الأدمن ملهوش أي صف في جدول `permissions`. ده مقصود (الأدمن الجذر = وصول كامل)، بس معناه إن `check_permission()` بيأثّر فقط على الأدمنية اللي ليهم صفوف صلاحيات صريحة. مهم تفهمه مع البند A4.

---

## 4. أولويات الإصلاح المقترحة

**الآن (أمني عالي):**
1. **A1** — حماية دخول الأدمن (rate-limit + قفل + reCAPTCHA حقيقية + 2FA).
2. **A2** — تفعيل CSRF + تحويل الحذف من GET لـ POST + ضبط SameSite.

**قريبًا:**
3. **A3** — تثبيت `cookie_secure = TRUE`.
4. **A4** — إضافة `check_permission()` للدوال الناقصة (خصوصًا تصدير CSV والإعدادات العامة).

**تحسينات/تكامل:**
5. **A5** — إخفاء البوابات غير المدعومة من واجهة الإعدادات وتوحيد إعداد PayPal.
6. **A6/A7/A8** — تنظيف تمرير JWT، ضبط الجلسة، توثيق نموذج الصلاحيات.

---

## 5. خلاصة إجابة سؤالك

- **«متربطة مظبوط بالمشروع؟»** → **أيوه**، اللوحة هي أداة الإدارة الحقيقية والوحيدة لبيانات الموقع (كورسات/تصنيفات/كوبونات/مستخدمين/اعتماد الكورسات/الإعدادات)، وكل ده بيتخزّن في نفس قاعدة البيانات اللي الـ SPA بيقرأ منها — فالربط سليم. الاستثناء الوحيد: **بوّابات الدفع** (اللوحة تقدر تفعّل بوابات لا يدعمها الـ checkout الفعلي).
- **«المشاكل فيها؟»** → أهمها **أمنية في اللوحة نفسها** (دخول بلا حماية brute-force، وCSRF متعطّل)، مش في ربطها بالمشروع.

---

*ملاحظة: التقرير مبني على مراجعة الكود (static review) في `c:\xampp\htdocs\body\api` + فحص إعدادات قاعدة البيانات الحية (نسخة `myco_uk`). لم يُجرَ استغلال حي. يُفضّل التحقق من كل بند على staging قبل أي إصلاح.*
